# 致命缺陷排查报告：旧设备白屏 + 冒烟测试虚假通过

> 日期：2026-08-29（21:00-22:20）
> 触发：执行项目自带运行时冒烟测试（`scripts/smoke.mjs`）时发现异常信号
> 结论：修复 1 个致命兼容性缺陷、1 个测试有效性缺陷、3 个既有阻塞错误

---

## 一、致命缺陷：es2022 产物在旧设备白屏

### 1.1 发现过程

冒烟测试输出「33 路由，异常路由 0」，看似全绿。但注意到一个**异常信号**：

```
[ok  ] /today/     root=453  mountCrash=0  ...
[ok  ] /companion/ root=453  mountCrash=0  ...
[ok  ] /letters/   root=453  mountCrash=0  ...
```

**33 个路由的 `root` 值完全相同（453）** —— 不同页面不可能内容一致，说明测的都是同一个静态占位。

深挖确证：页面等待 5~7 秒后 `#root` 仍只有 453 字节、`innerText` 为空 —— **React 根本没有水合**。

### 1.2 根因定位

用 CDP 注入错误钩子（`window.onerror` / `unhandledrejection` / `console.error`）捕获到：

```
Uncaught SyntaxError: Unexpected token '{'
  @ vendor-three-rt7E6wwV.js:1:9679
```

定位到该位置的代码：

```js
class Te{static{Te.prototype.isVector2=!0}constructor(e=0,t=0){...
```

**ES2022 类静态初始化块 `static{}`**。其浏览器支持门槛：

| 语法 | 最低支持 |
|---|---|
| `static{}` 类静态块 | **Chrome 94+ / Safari 16.4+**（2021-09 / 2023-03） |

而产物 target 为 `es2022`，需要 Chrome 94+。一个 chunk 语法错误会导致**整个 ES module 图加载失败**，React 完全无法挂载。

> 佐证：Node 22（现代 V8）解析同一文件**成功**，`curl` 下载 md5 与磁盘一致、MIME 为 `application/javascript`、无 BOM —— 排除了文件损坏与传输问题，确证是浏览器语法支持问题。

### 1.3 影响面

对**儿童学习 App** 是真实风险：家长常把旧平板 / 旧安卓设备给孩子用，这些设备浏览器版本普遍偏低。es2022 会直接导致：

- Chrome < 94、Safari < 16.4 的设备**打开即白屏**
- 无任何降级提示（静态占位会一直停留）

### 1.4 修复

`vite.config.ts`：`target: 'es2022'` → `'es2020'`

| 项 | 说明 |
|---|---|
| 覆盖面 | Chrome 80+ / Safari 13.1+ / Firefox 72+ |
| 体积代价 | gzip 266.78KB → 266.97KB（**+0.19KB**，可忽略） |
| 验证 | 新产物 `static{` 计数为 **0**；esbuild 以 `--target=chrome87` 转换**无报错** |

> `esbuild --target=chrome87` 无报错是重要旁证：若产物含顶层 await（Chrome 89+ 才支持），esbuild 会直接报错，故可排除。

---

## 二、测试有效性缺陷：冒烟测试「虚假通过」

这是本次排查中发现的**第二个真问题**——它让上述致命缺陷一直没被发现。

### 2.1 缺陷一：挂载判定过弱

```js
if (parsed.root > 300) break; // 已挂载
```

静态 LCP 占位本身就有 **453 字节**，`453 > 300` 恒成立 → React 完全没渲染也会被判定为「已挂载」。

### 2.2 缺陷二：失败判定只看崩溃

```js
const bad = mountCrashes.length || ix.crashes.length || crashedUI;
```

只要不抛错就算通过，**完全不检查是否真的渲染出内容**。

### 2.3 修复

1. 采集占位特征：`placeholder = root.innerHTML.includes('hero_jelly')`
2. 挂载判定改为：`root > 300 && !placeholder`（占位仍在则继续等到超时）
3. 未水合同样计为失败，输出 `NOT-HYDRATED` 标记：

```js
const notHydrated = !!r.placeholder;
const bad = mountCrashes.length || ix.crashes.length || crashedUI || notHydrated;
```

**效果验证**：修复后重跑，测试从 27 秒变为持续等待超时（每路由 14s）——证明新判定确实在工作（不再把占位误认为已挂载）。

---

## 三、顺带修复：3 个既有阻塞错误

改 `vite.config.ts` 触发了 `tsc -b` 全量检查，暴露出被**增量缓存掩盖**的既有错误：

| 文件 | 问题 | 修复 |
|---|---|---|
| `src/types.ts` | `DailyStat` 缺 `startLogicCorrect` 字段，但 `storeHelpers.ts` 已在读写（TS2339/TS2322） | 补 `startLogicCorrect?: number`，与 `startMathCorrect` 对称 |
| `src/App.tsx:86` | `Settings` 无 string 索引签名，直接断言 `Record` 报 TS2352 | 经 `unknown` 中转 |
| `src/App.tsx:178` | 多余的 `eslint-disable-next-line exhaustive-deps` 产生「未使用指令」告警，被 lint-budget 记为 `(unknown): 0→1` **导致门禁失败** | 移除（依赖 `[route, t]` 本就完整：`item` 由 route 派生、store 用 `getState()` 非订阅） |

---

## 四、验证结果

| 项 | 结果 |
|---|---|
| `tsc -b` | ✅ 0 错误 |
| `vite build` | ✅ 19.68s |
| `vitest run` | ✅ **1478 测试 / 181 文件全通过** |
| `npm run audit:contrast` | ✅ 0 处 ≤ 基线 0 |
| `npm run lint:budget` | ✅ CI 门禁通过 |

遗留（不阻断）：
- `webSpeechEngine.ts:106` prefer-const error —— **已知误报**（第 123 行确有赋值，改 const 会编译失败），lint-budget 明确标注「不计入门禁」
- `A11Y-CLICK-NO-ROLE: 1096 → 1097` —— 来自用户 WIP（PoemsPage 儿童化重构），信息性护栏不阻断

---

## 五、验证受限说明（需如实告知）

**运行时验证未能完成**，原因如下：

本机唯一可用于自动化的浏览器是 **Chrome 87.0.4280.88（2020 年，已停止更新）**，且排查后期环境退化：

- `chrome --dump-dom` 从返回 4850 字节退化为持续返回 **0 字节**
- CDP 通道反复 `channel closed`
- agent-browser 导航后 `location.href` 停留 `about:blank`
- Firefox 154 虽为现代版本，但 headless 截图未生成
- 系统无 playwright / puppeteer（无自带现代 Chromium）

因此 es2020 的兼容性由**静态证据链**支撑，而非运行时实测：

1. 新产物 `static{` 计数为 0（原产物有，且在 9679 列确证报错）
2. esbuild `--target=chrome87` 转换无报错
3. Node 22 解析新产物通过
4. 全量测试、构建、门禁通过

> **建议**：在 CI（GitHub Actions / ubuntu-latest + 现代 Chrome）复跑一次 `node scripts/smoke.mjs`，
> 确认 33 路由真实水合（不应出现 `NOT-HYDRATED` 标记）。修复后的冒烟测试此时才具备真正的判定能力。

---

## 六、附：本次排查的方法论沉淀

1. **警惕「全部相同」的测试数据**：33 个路由 root 值完全一致，是判定测试失效的强信号。
2. **测试通过 ≠ 功能正常**：断言过弱会让测试成为摆设，甚至比没有测试更危险（提供虚假安全感）。
3. **`tsc -b` 增量缓存会掩盖错误**：改动构建配置触发全量检查，是发现历史债务的时机。
4. **`pkill -f` 会匹配自身命令行**：用变量拼接（`P="remote-debug"; pkill -f "${P}ging-port"`）规避自匹配。
5. **ESM 不支持 `NODE_PATH`**：临时脚本需改用绝对路径 import。
