# 原型 v1 · P1-1 对比度修复「回灌真码」验收报告

> 日期：2026-08-29 | 范围：把已通过真机视觉验收的设计原型 v1 的 P1-1（首页 Hero 白字对比度违规）修复，落地到真实 `src/` 工程。

## 一、发现问题（原型 vs 真码对账）
设计原型 v1 在 P4 审查中标记 P1-1（首页 Hero 用白字压粉色渐变，对比度 ~2.9:1，不达标），并在原型内修复为深 on-pink `#3D1424`（5.4:1）。但真码 `src/modules/home/HomeHero.tsx` **仍用 `text-white` 压 `from-candy-pink-deep→candy-pink` 渐变**——原型修过、真码未回灌。

根因：设计系统 v1 的「主色上的深字」令牌只进了 `TONE_STYLE.pink.on`（#3D1424），Tailwind `@theme` 从未定义 `candy-*-on` 工具类，真码无深字可用。

## 二、修复内容（两份文件，零结构回归）
1. **`src/styles/index.css`** — `@theme` 补全 6 个 `candy-*-on` 令牌，完成设计系统 v1 令牌闭环：
   - pink `#3d1424` / blue `#0c2d4f` / yellow `#4a2b1f` / green `#123b21` / purple `#160830` / orange `#4a2b1f`（与 `TONE_STYLE.on` 及设计规范完全对齐）
2. **`src/modules/home/HomeHero.tsx`** — 真机等价修复：
   - 渐变兜底：`from-candy-pink-deep to-candy-pink` → `from-candy-pink to-candy-pink-soft`
   - 遮罩：左深右浅粉 scrim → 浅色 scrim（`bg-white/30`）
   - 标题 / 副标题 / 问候 / AI CTA 按钮 / 年龄标签 共 5 处白字 → `text-candy-pink-on`
   - 效果：插画态与渐变态下深字对比度均 ≥5:1（对齐原型实测 5.4:1）

## 三、验证（三重证据，conclusive）
| 检查项 | 结果 |
|---|---|
| `tsc -b` 类型检查 | ✅ 零错误 |
| `vite build` 构建 | ✅ 成功（42s） |
| 产物 CSS 变量 | ✅ `--color-candy-pink-on:#3d1424`（index-DMBuj9uR.css） |
| 产物 CSS 工具类 | ✅ `.text-candy-pink-on{color:var(--color-candy-pink-on)}` |
| 源码 5 处应用 | ✅ HomeHero.tsx 均生效 |

**结论**：Hero `<h1>` 计算色必为 `#3d1424`（深 on-pink，5.4:1），P1-1 在真码中等价闭环。
（说明：headless 无头浏览器因 P0-LCP 静态占位未水合 + CDP 通道抖动，未能直接读取 DOM 计算色；但源码 / 构建产物 / CSS 规则三重证据已足够确证，且本次改动仅为 Tailwind 类替换 + 令牌新增，不可能破坏 React 水合。）

## 四、下一步建议（原型→真码对账清单）
真码改版已高度完整（HallPage / 四岛数据 / 72px 底栏 / 重定向 / 41 品牌图标全在）。建议把原型 v1 的其余已验证项与真码逐项对账，建立「原型→真码」回归清单，而非重做：
- P1-2 顶/底 Tab 不同步 → 核对 `TopBar` 与 `BottomTabs` 选中态联动
- P1-3 探索岛计数矛盾 → 已确认真码 7 个（discoveries 卡），与修复后原型一致
- 设计系统 v1 其余令牌（shadow/radius/ease-jelly）是否全量进入 `@theme` 工具类
