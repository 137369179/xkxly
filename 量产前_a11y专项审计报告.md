# 量产前无障碍（a11y）专项审计报告

> 日期：2026-08-29 | 范围：重设计核心面（`home`/`hall`/`today`/`hanzi`/`growth` 模块 + `layout` 组件）
> 目的：设计原型 v1 → 真码对账闭环后，量产前基线的最后一关——确认可达性风险并给出整改清单。

## 一、扫描范围与方法
聚焦重设计核心面（非全站历史债务），按三类真实风险逐项 grep：
1. **对比度铁律**：白字是否压高饱和 main 色 / 浅柔色（deep 压白字合规）
2. **交互可达性**：非 `<button>` 的 `div`/`span` 带 `onClick`（缺角色语义）
3. **img 可达性**：`<img>` 缺 `alt`

## 二、结果总览

| 类别 | 核心面结果 | 结论 |
|---|---|---|
| 交互可达性（onClick 非 button） | 0 命中 | ✅ 干净（核心面统一用 `<CandyButton>`/`<button>`） |
| img alt | 0 命中（缺 alt） | ✅ 干净 |
| **对比度铁律** | **8+ 处白字压高饱和/浅柔色** | ⚠️ 唯一真实风险面 |

> 注：核心面「非 button 的 onClick」「img 缺 alt」与 08-29 早些时候专项审计「新文件 A11Y-CLICK-NO-ROLE 0 命中」一致，重设计新增代码无障碍语义干净。

## 三、对比度违规清单（按严重度）

### 🔴 HIGH（功能不可见）
- `HanziStrokeWriter.tsx:293` `bg-candy-blue ... text-white` —— `candy-blue` 在 `@theme` 解析为**浅柔蓝 #dcebff**，白字近乎不可见 → **本轮已修**（改 `text-candy-blue-on hover:text-white`，浅蓝底深字，hover 深蓝白字）

### 🟠 MEDIUM（~2.5–2.9:1，低于 WCAG AA 4.5:1）
- `HanziStrokeWriter.tsx:285` `bg-candy-pink ... text-white`（main 粉 #FF5C8A + 白 ≈2.9:1）→ **本轮已修**（改 `text-candy-pink-on hover:text-white`）
- `TodayPage.tsx:565` `bg-emerald-400 text-white`（浅绿 #34d399 + 白 ≈1.6:1）→ 待办
- `TodayPage.tsx:566` `bg-pink-500 text-white animate-bounce-soft`（pink-500 已被 @theme 覆盖为 #FF5C8A main，+白 ≈2.9:1）→ 待办
- `TodayPage.tsx:595` `bg-pink-500 ... text-white`（同上）→ 待办
- `TodaySections.tsx:99` `bg-amber-500 text-white`（amber-500 #F59E0B + 白 ≈2.1:1）→ 待办
- `hanzi/course/*` 多个 `from-amber-500 via-orange-500 to-amber-600 ... text-white` 渐变按钮（橙系高饱和 + 白 ≈2.5:1）→ 待办（HanziCourseRunner/HanziEtymologyPlay/HanziSpeechReview/HanziGamePractice 等）

### 🟢 合规（已确认）
- 所有 `bg-*-deep` 压白字（candy-pink-deep #C9285C / candy-orange-deep #B45F09 / candy-blue-deep #0B5EC9 / emerald-600/indigo-600/purple-600 等深色调，白字 ≥4.5:1）
- `MemoryConstellation` 深蓝紫渐变 + 白字（深色底）

## 四、根因
1. **设计系统令牌不对称**：`candy-pink` = main（#FF5C8A），但 `candy-blue` = soft（#DCEBFF）——同族命名约定不一致，导致 `bg-candy-blue` 是浅底，作者误配白字便不可见。
2. **对比度铁律无 lint 守卫**：规范写明「main 永不压白字」，但未被 lint 规则强制，靠人工易漏（本轮 P1-1、此处 HanziStrokeWriter 均为此类）。

## 五、整改路径（机制已就位）
- 设计系统 v1 的 **`candy-*-on` 深字令牌**已全量进 `@theme`（本轮补全），所有「main 色压字」场景应改用 `text-candy-*-on` 即可达标。
- 建议后续用脚本对 `bg-candy-<tone>` / `bg-<tone>-500` / `from-<tone>-500` 配 `text-white` 做一次性扫描替换（复用 `scripts/sweep-legacy-hex.py` 的同款预检机制），非本轮执行（波及 hanzi/today 多文件，需目检避免视觉副作用）。

## 六、本轮已落地
- 修复 `HanziStrokeWriter.tsx` 两处按钮对比度（HIGH + MEDIUM 最高危两项），用新 `candy-*-on` 令牌，零结构回归。
- 其余 MEDIUM 项列入待办，建议量产前一次性 sweep。

## 七、结论
重设计核心面**交互可达性与 img alt 已达标**；对比度是量产前唯一需收口项，根因是令牌不对称 + 缺 lint 守卫。最高危两项已修，机制（candy-*-on）已备，剩余 MEDIUM 项建议一次性 sweep 后上线。
