# 深度代码研究 · 剩余工作建议报告

> 对象：宝贝学习乐园（React 19 + Vite 7 + Tailwind v4 + Zustand）
> 日期：2026-08-29 · 方法：多维静态扫描（结构/TODO/性能/a11y/测试/依赖/资产/PWA）
> 现状基线：重设计已闭环（原型 v1 → 真码对账 ✅、a11y 审计 ✅、对比度 sweep + 守卫 ✅）

---

## 0. 工程体检概览

| 维度 | 现状 | 判定 |
|---|---|---|
| 规模 | 175 组件 / 303 模块文件 / 180 lib / 42 路由 | 庞大且健康 |
| 测试 | 1399 用例 / 179 文件（抽样 24/24 通过） | ✅ 覆盖良好 |
| 依赖 | 10 deps + 19 devDeps（three/opencc 为最大体积） | ✅ 精简 |
| 代码分割 | App 级 lazy + opencc 运行时动态 import | ✅ 已做 |
| PWA | manifest + sw + precache 构建时自动派生 | ✅ 完整 |
| 错误边界 | App/main/game/组件级 4 处 | ✅ 完善 |
| a11y | 交互可达性 0 命中、img alt 0 缺失 | ✅ 干净 |
| 对比度 | 核心面 0 违规；全域 278 基线已锁定 | ✅ 受控 |
| reduced-motion | index.css + 2 组件 | ✅ 已支持 |
| **资产体积** | **public 2.2GB**（hanzi-imgs 978MB + hanzi-videos 1.2GB） | ⚠️ **最大风险** |

---

## 1. P0 · 建议优先处理（高风险/高收益）

### 1.1 汉字图片资产未压缩：978MB PNG → WebP 可降 90%+
- **证据**：`public/hanzi-imgs/` 1278 张 1024×1024 PNG，单张最大 2.1MB（千/龙/故/微/龟），合计 978MB。
- **影响**：仓库体积 2.2GB → 拉取/CI/备份全慢；`dist` 构建副本 2.5GB；线上虽由 Cloudflare CDN 托管，但每张图 1-2MB 对移动端首屏/教学加载是真实卡顿源。
- **建议**：写一次性 codemod（沿用 `cwebp -q 82 -resize 512 512`，参考 brand3d 图标 47MB→0.24MB 的成功先例），1278 张 PNG → WebP，预计 978MB → **<100MB（-90%）**。
- **注意**：需同步改 `HanziLearn.tsx:165` / `HanziVideoCard.tsx:121` 的 `/hanzi-imgs/${char}.png` 引用为 `.webp`；保留一张基准对比图做视觉回归。

### 1.2 汉字教学视频 720p 偏高：1.2GB → 480p 可再降 ~40%
- **证据**：`public/hanzi-videos/` 1278 个 MP4（H.264 High, 1280×720, 116kb/s + 93kb/s AAC, 50s 时长），均值 0.9MB，合计 1.2GB。
- **影响**：汉字笔画教学属低动态内容，720p 明显过度；移动端流量与 CDN 成本双高。
- **建议**：ffmpeg 批量转 **480p（854×480, crf 26~28, 视频 80kb/s）+ AAC 64kb/s**，预计 1.2GB → **~600MB（-50%）**。保留一个样本人工比对清晰度。
- **收益**：两项合计 public 2.2GB → **~0.7GB（-68%）**，部署/Pages 瘦身步骤也可简化。

### 1.3 ~~`emptyOutDir:false` 长期隐患~~ → **已更正：不应改动**
> ⚠️ 本条为初稿误判，复核 `vite.config.ts:53-59` 与 `scripts/clean-dist.mjs` 后更正。

- **真相**：`emptyOutDir:false` 是**刻意为之**。注释明确记载：本机安全工具会拦截 vite 对 `dist/icons` 等子目录的**批量删除（>50 文件）**，导致 build 在 `prepare-out-dir` 阶段失败；`clean-dist.mjs` 正是为此改用逐文件 `unlinkSync` 绕过保护。
- **结论**：资产瘦身后 dist 由 2.5GB 自然降至 ~0.8GB，问题已大幅缓解；**恢复 `true` 会让 build 因批量删除保护而中断**，属引入风险。**建议保持现状**，仅补充注释说明「媒体已瘦身」。

---

## 2. P1 · 建议下一批做（明确收益）

### 2.1 对比度债务逐模块收窄（278 处 → 机制已就位，按量收割）
- **证据**：全域 278 处违规中 modules 占 243：**science 44 / pet 39 / music 17 / letters 17 / storybook 14 / pinyin 12 / numbers 12 / logic 11 / safety 10** …
- **建议**：守卫 + codemod 已可用，按「量从大到小」每模块跑一次 sweep（science 44 → pet 39 → …），**每次可降基线 10-40 处**；纯机械替换 `text-white`→`text-candy-*-on`，风险低。这是当前性价比最高的持续改进。

### 2.2 ~~vendor chunk 体积 / science-pet 路由 lazy~~ → **已更正：无需改动**
> ⚠️ 本条为初稿误判，复核 `src/Page.tsx` 与 `vite.config.ts:62-66` 后更正。

- **真相一**：`src/Page.tsx` 已有 **43 个页面全部 `React.lazy`**（含 `SciencePage` L27 / `CatHousePage` L35 / `RealisticCatHousePage` L36），three 只在进入 science/pet 页时加载，**不进首屏主包**。
- **真相二**：`vite.config.ts` 已配置 `modulePreload` 剔除正则，把 `vendor-three|vendor-opencc|data-poems|vendor-pinyin|data-hanzi|confetti` 排除在静态预取之外（注释说明避免了首屏约 2.4MB 无效流量）。
- **结论**：打包优化**已到位**，无需改动。仅剩的 `chunkSizeWarningLimit` 警告属「大 chunk 但已按需 + 不预取」的良性状态。

### 2.3 首页体验补强（改版后「一屏一事」已达标，但仍有 premium 空间）
- **证据**：首页 = HomeHeader + HomeHero + 3 快捷位 + 乐园入口，极简达标 ✅。
- **建议（可选）**：Hero 今日任务卡片增加**实时进度**（今日已完成 X/3 个任务，读 `useDailyLog`）——让「今天做什么」从静态入口升级为「今天做到哪」的激励反馈；成本低、教育价值高。

### 2.4 乐园地图动效深化（原型 v1 已验证，真码偏静态）
- **证据**：`HallPage.tsx` 岛屿卡仅 `transition-transform active:translate-y-1`，无入场/悬浮动效；原型 v1 有 jelly 入场。
- **建议**：岛屿卡加**入场 stagger**（`animate-float` 已有 + 错峰 delay）与 hover 轻微上浮，完全复用 v1 令牌（`--ease-jelly` 本轮已补进 `@theme`），零新增依赖。

---

## 3. P2 · 可选优化（低优先）

| 项 | 证据 | 建议 |
|---|---|---|
| deprecated API 清理 | `useStore.ts` 3 处 `@deprecated`（细粒度 selector）、`ProgressBar` 3 处兼容别名、`ParentBackupSection` 1 处 | 顺手清理或加 lint 标记，防新代码误用旧 API |
| 繁体 locale 首切体验 | opencc 动态加载，zh-TW 首切有瞬时加载 | `ensureCn2t()` 可在 idle 期预热（已有入口） |
| 大文件单项提示 | 单图 1-2MB（未优化前） | 1.1 优化后自动缓解 |
| 全局点读增强 | speech/tts 管线已完整（`lib/speech.ts` + `tts/manager.ts`） | 若已有全局点读则跳过；无则考虑 Phase D 补全 |
| 测试补齐 | 179 文件 1399 用例 | 对 sweep/资产转换等改动补回归用例即可 |

---

## 4. 结论（按建议执行顺序）

1. **（P0）资产瘦身一刀**：1278 PNG → WebP（978MB→~90MB）+ 1278 视频 → 480p（1.2GB→~0.6GB）→ public 2.2GB→~0.7GB，随后恢复 `emptyOutDir:true`。
2. **（P1）对比度按模块收割**：science → pet → letters → numbers，守卫每轮自动刷新基线。
3. **（P1）路由级 lazy**：science/pet 页懒加载，three 退出首屏主包。
4. **（P1/P2）体验**：Hero 今日进度 + 乐园地图 stagger 动效（复用 v1 令牌）。
5. **（P2）工程债**：deprecated API 清理、繁体预热。

> 注：1.1/1.2 属**资产格式迁移**（与品牌图标 WebP 优化同型），非破坏性代码改动；如需执行，先干跑生成样本比对，再全量。
