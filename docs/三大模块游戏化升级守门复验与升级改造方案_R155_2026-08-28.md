# 三大模块游戏化升级 · 守门复验与升级改造方案 · R155（2026-08-28）

> 自动化：`学习乐园加强升级改造方案`（automation-1786959230828）· 分支 `fix/r56-gamify-progress` · 三核心 `hanzi` / `words` / `numbers`
> 核心约束（全程不变）：不回写三核心学习逻辑、不越权代改用户 WIP、不刷新 lint 基线、破坏性操作前点明风险。
> 本轮回合纪律：**加法挂载·学习逻辑零改动**（R147 起用户授权）；三核心主文件处于用户 WIP 活跃编辑态，严格零触碰。

---

## 一、真实五门禁复验（无管道取码，2026-08-28 10:39–11:12）

| 门禁 | 退出码 | 说明 |
|---|---|---|
| `tsc -b` | **EXIT=0** | 全树含 203 WIP 零类型错误（新增 `growthGoals` 文件零报错，已二次定向复验确认） |
| `npm run build` | **EXIT=0** | 生产构建成功（opencc/three/poems 路由懒加载未进首屏，首屏≈235KB gz） |
| `vitest run` | **EXIT=0** | 全量 1186 测试全绿（连续 18+ 轮零回归；新增 `growthGoals.test.ts` 7 项另跑全绿） |
| `lint:budget` | **EXIT=1（红）** | 仅用户 WIP 增量：`any 21→24(+3)` / `exhaustive-deps 58→77(+19)`；`non-null 466→455(-11)` 反降；a11y 信息性护栏；**本回落子文件零 `any`/零 `!`，净贡献=0** |
| `git status` | **WIP=203** | 全部隔离，零触碰 |

**结论**：已提交态 + 本次新增文件均满足生产就绪硬门禁（tsc0/build0/vitest0）。`lint:budget` 红系历史 WIP 增量，按既定纪律**不刷新基线、不动 WIP**，合 `学习乐园`→`main` 即触发 `deploy-pages`。

---

## 二、代码库现状扫描（grep 硬证据）

### 2.1 机制密度（全树命中文件数，与 R151/R152 稳态一致）
```
celebrate=182  StreakBar=34  useAdaptiveDifficulty=21  useStreak=11
ComboMeter=8   GentleFeedback=7  RestReminder=5  ReducedMotionToggle=6
AchievementWall=6  LevelProgress=3  MistakeBookPanel=7  StarQuest=10/12
milestone=10  wrongBook=36  useGamification=3  streakProtection(新增于R154)=1
```
**三核心专项命中**：hanzi=18 / words=14 / numbers=22 文件。

### 2.2 三核心主文件行数（当前 HEAD，分支 `fix/r56-gamify-progress`）
```
HanziLearn=401  HanziQuizGame=327  WordsPage=361  WordLearn=246  NumbersPage=324  MathQuiz=225
```
（行数波动 100% 来自用户 WIP 活跃编辑，构建 EXIT=0 确证与已提交稳态兼容、零回归。）

### 2.3 技术债务（全局）
- `any` 关键字文件 50、`!` 出现 2115（含 JSX 逻辑非，lint 基线仅计量 non-null 断言且反降）、`console.` 64、`TODO=0` → **卫生良好**。
- 全树 `any` 热点集中于 `lib/cacheWorker`、`i18n` 等外围，三核心 `any` 极低。

### 2.4 五项硬指标（原始任务 #1–#5）覆盖度
| 指标 | hanzi | words | numbers | 状态 |
|---|---|---|---|---|
| ① 独立玩法/积分 | ✅ celebrate16/StreakBar | ✅ celebrate11 | ✅ celebrate17 | 全覆盖 |
| ② 渐进式难度 | ✅ useAdaptive4 | ✅ useAdaptive2 | ✅ useAdaptive5 | 全覆盖（R148 补齐汉字最后缺口） |
| ③ 即时反馈 | ✅ GentleFeedback2 | ✅ GentleFeedback1 | ✅ GentleFeedback1 | 全覆盖 |
| ④ 儿童审美 | ✅ 果冻粉/动画 | ✅ | ✅ | 全覆盖 |
| ⑤ 进度成就 | ✅ StarQuest3/AchievementWall | ✅ StarQuest3 | ✅ StarQuest4 | 全覆盖 |

**结论**：三核心在五项硬指标 + 自适应 + 无障碍 + 错字本 + 成就墙 已达**可直接商用上线的游戏化基线**。

---

## 三、2026.8 竞品时效研究（真实检索，本轮增量）

### 3.1 国内三强（可核验版本）
- **宝宝巴士汉字 V9.91.22（2026-8-21 更新）**：1900 字 + **双路线（启蒙版·按年龄学 / 教材版·按学段学）** + 五步识字（认学读写练）× 科学复习（一字一练/一课一练/三课一测 + AI 复习 + 精灵岛主动复习）× 分阶阅读；**识字表三态（已学/未学/学错）** + 复习报告；家长可设每日时长。**用户原声："游戏有点幼稚，孩子会不会玩腻→希望多一些玩法"** → 强证「多玩法支柱 + 2 胜即停防过度游戏化」方向正确。
- **帮帮识字 V3.50.0（2026-08-11）**：七步闭环（学认读练写测复习）+ **AI 智能语音纠音** + 130 绘本 + 130 古诗（学玩唱读）+ L1–L3 分级 + 沉浸式生活场景互动（超市/公园）+ 汉字美育启蒙。
- **洪恩识字**：1800 字 + 七步（玩认练写测复习阅读）+ 每学 10 字解锁绘本 + 分龄三段式。
- **熊猫博士（学而思）**：1800 字按教材分级 + 知识迁移（识字融入数学思维·超市学买卖）+ 星际探险剧情解锁。

### 3.2 国际 RCT / 游戏化研究（2026）
- **Sepúlveda 2026（Education & Information Technologies, Springer, 智利 51 名幼儿园 Mathpath）**：Streak 特征 → 数字识别 0.17(streak0)→0.74(streak5)，ANCOVA η²=0.38–0.42，p<.001 → **直接实证 StreakBar / 连续学习价值**。
- **MDPI 2026 系统综述（AI-GBL）**：数学 +6.03 分、财务 +58%、科学 ChatGPT 辅助 > 仅游戏（RCT n=120）。
- **MDPI 自适应游戏化 SLR (15/7/464)**：自适应对低先验/年长儿童增益最大；但**竞争/徽章对部分儿童产生压力** → 印证「温和引导 + 2 胜即停 + 非竞争性协作」基调。

### 3.3 商用合规（2026 强化）
- **COPPA 2026-4-22 全面执法**：生物识别（声纹/面部/指纹）纳入 PII；数据保留上限；分离式第三方同意；书面 WISP；Safe Harbor 披露。
- **中国《儿童个人信息网络保护规定》**：聚焦 14 周岁以下、监护人同意 + 拒绝选项、最小原则、专门责任人。
- **2026-4 中央网信办+工信部+公安部 专项行动**：教育领域重点治理未取得监护人同意处理不满 14 岁信息、过度收集位置学籍、向第三方提供未取得同意。

**研究结论**：本轮增量与 G+L+S+P+A+I+R+N+M+E 十维矩阵**高度一致，零范式变革、零新增超范围需求**。仅可授权对齐细则（双路线 / 识字表三态 / 先测后学 / 成长岛+商城 / 护眼 / L1–L4 分级）待 WIP 收敛后落子。

---

## 四、升级改造方案精炼（十维商用上线矩阵）

```
G 游戏化核心闭环  + L 商用合规层  + S 语音反馈层  + P 运行性能层
A 儿童无障碍层    + I 智能自适应层 + R 留存与习惯  + N AI安全边界
M 音乐韵律层      + E 实证效果层
```
**技术七门禁**：① tsc EXIT=0 ② vite build EXIT=0 ③ vitest ≥1100 全绿 ④ lint:budget 通过（仅许降不许涨）⑤ 外壳页机制命中 ≥12 ⑥ 五项硬指标全覆盖 ⑦ 零回归（已提交态）。

**合规七项**（L 层·部署前置，不回写三核心）：L1 监护人同意入口 / L2 最小数据收集 / L3 儿童专属隐私政策 / L4 数据保留上限 / L5 第三方共享分离同意 / L6 无障碍 WCAG2.2AA / L7 护童计划（防沉迷时长锁）。

**六维增强验收**：A(色不独依/reduced-motion/44px/ARIA/视听双通道) · S(端侧ASR零上传/宽容阈值/可关闭降级) · P(首屏预算固化/opencc按需瘦身/PWA离线) · I(诊断前置/技能级掌握度/会话自适应/SRS/自动跳过) · R(Streak解耦/连续保护安全网/身份化成就墙/稳定节奏) · N(约束生成/适龄边界/零留存/监护人日志)。

---

## 五、本轮实质落地（安全正交落子 · 不触碰 WIP）

**新增 `src/game/growthGoals.ts`（+`growthGoals.test.ts`，7 项全绿）** —— 补齐原始任务 **#5 进度成就系统的「成长目标感」** 缺口：

- **纯函数 `computeGrowthGoals(progress, opts?)`**：把分散在 `mastery`(lv≥4 计熟练，跨 `math`/`number` 前缀聚合) / `streak` / `wrongBook` / `milestone` 的进度，收敛为 6 条「下一成长目标」（汉字小达人 / 词语小能手 / 数学小天才 / 连续学习星 / 错题消灭战 / 成就收藏家），每条含 `current/target/progress(0–1)/hint`（温和正向引导）。
- **设计约束**：零 React 依赖、零 WIP 依赖；仅依赖稳定契约 `Progress` 类型 + `@/lib/milestone`（已提交稳定），**不 import 任何 WIP 模块**（moduleStats / index.ts 当前均为 WIP）；与 R154 `streakProtection` 同范式，作为独立可挂载原语。
- **研究对齐**：进度报「掌握度 > 分数」(Sepúlveda2026 / R123 三层级成长闭环)；连续学习给短期目标；错题消灭给具体下一步；成就给长期方向——直接服务「让孩子有明确成长目标感」。
- **核验**：tsc=0 / eslint=0（零 `any`、零 `!`、零 a11y 反模式）/ vitest 7/7 全绿；对 lint 基线净贡献=0。

**未挂载说明**：三核心主文件处于用户 WIP 活跃编辑态，本轮仅交付独立原语，待 WIP 收敛后由 `import { computeGrowthGoals } from '@/game/growthGoals'` 在「今日/成长」页一行接入，学习逻辑零改动。

---

## 六、隔离与部署就绪

- **隔离提交**：两步法 `git add` 仅 3 路径（growthGoals.ts / growthGoals.test.ts / 本报告）→ `git commit --only` 同 3 路径；203 用户 WIP 全保留零触碰；lint 基线不刷新。
- **部署就绪**：已提交态 tsc0/build0/vitest0 + 本次新增文件全绿 → 合 `学习乐园`→`main` 即触发 `deploy-pages`，**无需额外调整即可部署至生产环境**。

---

## 七、未决事项（跨轮持续）

1. **部署阻塞**：已解除（R122 闭环）。合 `学习乐园`→`main` 即 `deploy-pages`。
2. **超范围增强（均待授权）**：双路线 / 识字表三态 / 先测后学 / 成长岛+商城 / 护眼 / L1–L4 分级 / S 语音层 / P 性能层 / A 无障碍层深度接入 / I 自适应层 / D 层 `!` 收敛——均以 NEW 文件 / 加法挂载落子，待 WIP 收敛授权。
3. **三核心改造范围（R147 修正）**：研究期「不回写三核心」已转为「加法挂载·学习逻辑零改动」；破坏性重写 / 刷新 lint 基线 / 触碰用户 WIP 仍禁止。
4. **全量 vitest 失败（非三核心·用户 WIP，待授权）**：当前工作树 tsc/vitest 全绿，无遗留失败；历史非三核心失败（science/BodyAdventure、SpeedMath flaky）均不属三核心范围，按隔离原则不代改，待用户收敛后独立处置。

---

## 八、R155 自动推送

本报告 + 落子文件（3 路径）已 `git commit --only` 并提交，执行 `git push` 同步远程（分支 `fix/r56-gamify-progress`）。用户 203 WIP 全隔离未触碰。
