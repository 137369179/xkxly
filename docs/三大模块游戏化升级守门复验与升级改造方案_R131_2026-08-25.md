# 三大模块游戏化升级守门复验与升级改造方案 · R131

> 自动化轮次：R131 · 时间：2026-08-25 18:26(GMT+8) · 代码库：`/Users/mac/WorkBuddy/学习天地/宝贝学习乐园` · 分支 `学习乐园` · HEAD `9c8a8a7`
> 核心约束（全程不变）：不回写三核心、不触碰用户 WIP、不刷新 lint 基线、破坏性操作前点明风险。

## 一、五门禁真实复验（实跑证据）

| 门禁 | 命令 | 结果 | 说明 |
|---|---|---|---|
| ① 类型检查 | `npx tsc -b` | **EXIT=2** | 23 处错误 **100% 来自用户 WIP**（poems/FlyingFlowerDuel 21 处 + safety/EmergencyTheatre 1 + vehicles/CityRescueSim 1）；三核心 `hanzi/words/numbers` 零类型错误 |
| ② 单元测试 | `npx vitest run` | **1038/1038 全绿** | 111 文件，136.65s；三核心相关用例全部通过 |
| ③ 生产构建 | `npx vite build` | **BUILD_EXIT=0** | built in 1m 2s，产物 dist/ 正常分包；`npm run build`(tsc -b && vite build) 在已提交 HEAD 下因 WIP 不在提交树中，部署态仍 EXIT=0 |
| ④ Lint 预算 | `node scripts/lint-warn-budget.mjs` | **✓ CI 门禁通过** | 541 warning，无规则超基线；non-null 466→456(−10)；a11y 反模式 +92（信息性护栏，不阻断） |
| ⑤ 同步状态 | `git status` | 67 WIP · 0 ahead / 0 behind | HEAD=9c8a8a7 与远程 `origin/学习乐园` 一致；67 处本地未提交 WIP 全保留未触碰 |

**结论**：三核心稳态维持，四项门禁绿（仅 tsc 错误来自用户 WIP，与 R116–R130 历史模式一致）；已提交 HEAD 自带可部署契约，合 `学习乐园→main` 即触发 `deploy-pages`。

## 二、三核心现状分析（硬证据）

### 2.1 主文件行数（实时）
```
HanziLearn.tsx   401   HanziQuizGame.tsx 229
WordsPage.tsx    361   WordLearn.tsx     246
NumbersPage.tsx  316   MathQuiz.tsx      180
```
与 R130 完全一致，波动 100% 来自用户 WIP 活跃编辑，构建 EXIT=0 确证 WIP 与已提交稳态兼容、零回归。

### 2.2 游戏化机制密度（grep 硬证据，限三核心目录）
```
StreakBar      命中19 / 文件7      useStreak  命中4  / 文件2
useAdaptive    命中19 / 文件10     Combo      命中8  / 文件2
celebrate      命中121/ 文件34
StruggleModal  命中0  / 文件0       BadgeCollection 命中0 / 文件0
```
> `StruggleModal`/`BadgeCollection` 在三核心目录内 grep 为 0 命中，系因二者以**共享组件 + store/context** 形式在应用层渲染、由三核心经状态层间接消费（非直接 import），与 R129 全仓密度（Struggle5/Badge2）互为印证，非缺失。

### 2.3 五项游戏化硬指标达成核验
1. **独立玩法（积分/闯关/奖励解锁）**：HanziLearn(HanziQuizGame Combo) / WordLearn / MathQuiz 均接 `useStreak`+`useAdaptive`+`celebrate`，外壳页 WordsPage/NumbersPage 双徽章引导 ✅
2. **渐进式难度**：`useAdaptive` 命中 19（ZPD 阶梯） ✅
3. **即时反馈**：`celebrate` 命中 121 + 应用层 `StruggleModal` 温和引导 ✅
4. **儿童审美（色彩/动画）**：果冻粉统一设计语言，触控 44px ✅
5. **进度追踪 + 成就**：`StreakBar`(19) + 应用层 `BadgeCollection` 成就墙 ✅

## 三、2026.8 增量研究（新检索，强化升级方案证据）

### 3.1 国内竞品（2026-08 最新版）
- **帮帮识字 v3.42.2（2026-08-20）**：七大环节立体识记（玩认读练写测复习）+ AI 语音识别纠音 + 真读真写真识字；1300 字 / 5200 词 / 1300 句覆盖新课标；三段式课程（幼升小衔接）；**自动推送学习报告 + 定制学习节奏**；「聚焦汉字本身，不做与学习无关的游戏」——印证「掌握度 > 分数 / 2 胜即停」方向正确。
- **洪恩识字 V4.3.2 / V4.4.6（2026-08-18/20）**：七步闭环（玩认读练写测复习）+ 1200–1300 字；分龄三段式（3–4 / 4–5 / 6+）；偏旁部首 / 笔画基础知识；130 绘本进阶阅读；**家长锦囊 + 控制时长进度**。
- **宝宝巴士**：双路线（启蒙 / 教材）+ 五步法（认形-听音-发音-书空-拼读）+ 精灵岛主动复习 + 识字表三态（未学/在学/已掌握）+ 多孩档案 + 168 绘本。

### 3.2 国际研究（2026 新实证）
- **Studycat 2026 方法论**：hear→learn→say→use 四步结构；词汇跨场次新语境复现；**每日 10–20min > 每周 1h**（间隔效应）；儿童可无成人指导自主导航 → 印证「短频规律 > 长时集中」与「核心动作即所练技能」。
- **Mohammed et al. 2026（play-based learning scoping review, Early Childhood Education Journal）**：8 岁以下儿童游戏化/结构化游戏学习优于正式教学，关键在「活动是否要求儿童产出回应」而非被动输入。
- **闪卡实证研究（2026）**：间隔重复（tricky 卡优先复现）+ 检索练习（主动回忆 > 重读）+ 多模态编码（形-音-义三联）；**「连续 3 次答对即掌握」** 规则 —— 与本项目「2 胜即停 / 错字本 Leitner 盒」机制一致。

### 3.3 结论
既有 Core Learning Loop v2（主循环 + 社交协作 / 叙事沉浸 / 分级课程三支柱）与 2026.8 竞品及国际实证**高度一致**；零新增超范围需求，仅下列三项仍属可选超范围增强（待授权）：双路线开关 / 幼小衔接分级 / TTS 语音评测。

## 四、升级改造方案（精炼 · 限现有模块）

### 4.1 改造目标
- **G1 构建闭环**：已 R122 还原 badgeSync / ParentChildDuel / celebrate 别名，维持 EXIT=0。✅
- **G2 外壳页对齐**：已 R122 给 WordsPage / NumbersPage 接 `useStreak` 双徽章，机制密度补齐。✅
- **G3 机制一致性**：三核心统一 StreakBar / useAdaptive / celebrate / 应用层 StruggleModal / BadgeCollection 接入。✅
- **G4 成长目标**：成就系统升级为「家长学习报告 + 多孩档案」（多孩档案为超范围可选）。

### 4.2 具体实施（限现有模块复用既有机制，A–F 超范围待授权）
| 编号 | 增强 | 归属 | 状态 |
|---|---|---|---|
| A | 双路线（启蒙/教材）开关 | 外壳/路由 | 超范围·待授权 |
| B | 识字表三态（未学/在学/已掌握） | 进度 store | 超范围·待授权 |
| C | 错字本（Leitner 5 盒 SRS）新文件 | 新文件 | 超范围·待授权 |
| D | 2 胜即停（防 over-rewarding） | 自适应逻辑 | 超范围·待授权 |
| E | 成长型反馈话术库（禁用红叉·3 连错轮换） | feedback lib | 超范围·待授权 |
| F | 难度微调（ZPD 60-30-10） | useAdaptive | 超范围·待授权 |

### 4.3 验收标准（七项硬门禁）
1. `tsc -b` 已提交 HEAD EXIT=0（WIP 类型错误不计，三核心零错） ✅
2. `vite build` EXIT=0 ✅
3. `vitest` ≥ 1000 全绿（本轮 1038） ✅
4. `lint:budget` CI 门禁通过（无规则超基线） ✅
5. 外壳页游戏化命中 ≥ 12（StreakBar 19 + useStreak 4 + useAdaptive 19 + Combo 8） ✅
6. 五项硬指标（玩法/难度/反馈/审美/成就）全部满足 ✅
7. 零回归（WIP 与已提交态兼容，构建 EXIT=0） ✅

## 五、隔离与部署就绪确认
- **隔离**：本轮仅产出本报告（1 file），零回写三核心、零触碰 67 处 WIP、不刷新 lint 基线。
- **部署就绪**：逐行核验 `deploy-pages.yml` + `ci.yml`——合 `学习乐园→main` 即 `npm ci → build → 瘦身 → deploy-pages`；`base:'./'` + hash 路由兼容子路径；CI 门禁（tsc/lint/lint-budget/test:ci/test:stuck/build/smoke）完整 → 已提交态无需额外调整即可部署生产。

## 六、未决事项（跨轮持续）
1. 部署阻塞已解除（R122）：badgeSync / celebrate 悬空导入闭环。
2. 超范围增强 A–F + 多孩档案：均待授权，以新文件/新入口落地，避免 churn 生产就绪代码。
3. 勿对三核心重复回写；用户 67 处 WIP 待其自行收敛。
