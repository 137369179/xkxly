# fun 打地鼠游戏化升级报告（R55 · 分数接入全局星星）

> 执行时间：2026-08-21 20:14–20:35
> 代码库：`/Users/mac/WorkBuddy/学习天地/宝贝学习乐园`（分支 `学习乐园`）
> 轮次性质：用户「继续」→ 按 R52 蓝图 B2（fun 模块）实施游戏化

---

## 一、现状分析（改造前，实测）

`WhackAMole.tsx`（239 行）：
- 有本地 `score` + `best`（localStorage `whack-best`）——**孤岛数据**，未接入全局成就体系；
- 游戏结束仅庆祝动画，无星星入账、无 SRS/成长记录；
- 无任何测试覆盖。

## 二、参考设计（R52 蓝图 B2）

- **玩法**：WhackAMole 分数实时换算星星入账（消除本地孤岛，接入全局 `addStars`）；
- **保留**：本地最高分作展示（best 继续显示）。

## 三、具体实施内容

`src/modules/fun/WhackAMole.tsx`：
1. 接入 `useStore(s => s.addStars)`；
2. 游戏结束时按 `score × 0.5` 换算星星入账（`Math.max(1, ...)` 保证至少 1 星，30 击满分约 15 星）；
3. 结果面板显示「⭐ 收获 N 颗星星！」即时反馈；
4. 修复 `useEffect` 缺 `addStars` 依赖的 lint warning。

`src/modules/fun/WhackAMole.test.tsx`（新增 2 例）：
- 渲染开始面板 + 开始按钮；
- 点击开始后渲染 3×3 地鼠格子。

## 四、验收标准与实测结果

| 门禁 | 结果 |
| --- | --- |
| TypeScript | 验证中（排除并行 WIP） |
| ESLint | **0 error**（exhaustive-deps 已修复） |
| 单元测试 | WhackAMole 2/2 通过 |
| 生产构建 | 待全量验证 |

## 五、范围合规

- ✅ 仅修改 `WhackAMole.tsx` + 新增测试；
- ✅ 未新增依赖、未触碰并行会话 WIP（server/ai 模块）；
- ✅ 复用既有 `addStars` 全局星星体系（消除 localStorage 孤岛）。

## 六、后续可选（需确认）

ParentChildPK 亲子对战计入连对 · Nursery 跟唱完成点亮 · fun 模块其他游戏接入星星/徽章。
