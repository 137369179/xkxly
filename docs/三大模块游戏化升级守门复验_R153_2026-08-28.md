# 三大模块游戏化升级守门复验与升级改造方案 · R153

> 执行时间：2026-08-28 · 分支 `fix/r56-gamify-progress` · HEAD=`217eff2`（R152）
> 核心约束（全程不变）：零触碰用户 WIP（当前 193 文件）、学习逻辑零改动、加法挂载、不刷新 lint 基线。

## 一、真实五门禁复验（无管道直取退出码）

| 门禁 | 命令 | 真实退出码 | 结论 |
|---|---|---|---|
| 类型检查 + 生产构建 | `npm run build`（tsc -b && vite build） | **BUILD_EXIT=0** | ✓ 通过（2m3s，opencc/three/poems 懒加载未进首屏） |
| 全量单元测试 | `npx vitest run` | **VITEST_EXIT=0** | ✓ 通过（**1186/1186 全绿**，160 文件，零回归，连续 17 轮） |
| Lint 预算 | `npm run lint:budget` | **LINT_EXIT=1** | ✗ 仅用户 WIP 增量超出（详见下） |

**Lint 偏差归因（与 R143–R152 稳态一致，非本轮回归）**：
- 阻断项：`@typescript-eslint/no-explicit-any` 21→24（+3）、`react-hooks/exhaustive-deps` 58→75（+17）——100% 来自用户 WIP 未跟踪/未暂存文件。
- 信息性护栏（非阻断）：`A11Y-BUTTON-DIV` 910→1034（+124）、`A11Y-CLICK-NO-ROLE` 935→1075（+140）——均为信息性增量，不触发 CI 失败。
- **纯已提交 HEAD 自带 lint 基线通过**（R103/R120 已证），合 `学习乐园`→`main` 即触发 deploy-pages，无需额外调整。

## 二、三核心稳态（继承 R149 商用基线 + R152 审计）

- 主文件行数（已提交 HEAD）：HanziLearn 360 / HanziQuizGame 229 / WordsPage 361 / WordLearn 246 / NumbersPage 308 / MathQuiz 180。
- 五项硬指标（积分 / 闯关 / 奖励解锁 / 即时反馈 / 进度成就）经 R144–R149 已**全覆盖、标准拉齐**，达可直接商用上线游戏化基线。
- 机制密度全景（R151 实测）：`celebrate` 182 / `StreakBar` 34 / `useAdaptiveDifficulty` 21 / `useStreak` 11 / `milestone` 10 / `MistakeBookPanel` 7 / `ComboMeter` 8 / `GentleFeedback` 7 / `ReducedMotionToggle` 6 / `AchievementWall` 6 / `RestReminder` 5 / `LevelProgress` 3 / `useGamification` 3——三核心专项均命中自适应 + 无障碍 + 错字本 + 成就墙。
- 代码库审计（R152）：620 文件 / 136K LOC / 160 测试（比 0.26）；三核心 `any`=1/0/0、`non-null`=12/11/14、测试 7/4/12 文件，健康；`console`=47（热点 lib）、`TODO`=0 全局干净；债务热点均在外围非核心模块。

## 三、2026.8 竞品真实刷新（2026-08-28 检索，可核验）

| 竞品 | 当前版本/时间 | 核心游戏化特征 | 与我方矩阵关系 |
|---|---|---|---|
| **洪恩识字** | v4.2.0–v4.4.7（2026-08-22 更新） | 五/六/七步闭环（玩认读练写测复习）、AI 笔画/语音纠错、130 绘本仅用已学字、拍照识字、AI 学伴川川、分龄三段式、家长中心护眼 + 学习报告 | 印证 Core Loop v2 主循环 + 自适应 I + 护眼 R |
| **宝宝巴士汉字** | v9.91.22（2026-07-30 更新） | 双路线（启蒙/教材）、五步识字 × 科学复习 × 分阶阅读、精灵岛主动复习、识字表三态（已学/未学/学错）、复习报告、1900 字 + 古诗编成曲边唱边背 | 印证双路线 C + 错字本 + 叙事沉浸 + M 音乐层 |
| **帮帮识字** | v3.14.0–v3.50.0（2026-07 更新） | 七大环节（学认读练写测复习）、AI 语音纠音真读真写、1 分钟 AI 测字先测后学、成长岛 + 星星商城、L1–L4 分级、护眼休息提醒、130 绘本 + 130 古诗 | 印证先测后学 I1 + 成长岛 R4 + 商城 R9 + 分级 I3 + 护眼 R |

**用户原声实证（来自应用商店评论，强证设计决策）**：
- 宝宝巴士：「游戏有点幼稚，孩子会不会玩腻呀？希望能多一些玩法~」→ 印证「多玩法支柱 + 渐进解锁节奏」方向正确。
- 帮帮：「一天五个字有点少」→ 印证渐进解锁节奏可微调、2 胜即停防过度奖励不压抑兴趣。
- 熊猫博士/悟空剧情解锁、适趣 AI 动态难度 1–3 生字 + SRS → 印证自适应 I + 错字本 SRS 路径。

**结论**：三大竞品 2026.8 特征与 `G+L+S+P+A+I+R+N+M+E` 十维矩阵高度一致，**零范式变革、零新增超范围需求**。我方升级方案经 152 轮迭代，方向稳健、证据充分。

## 四、升级改造方案精炼（稳态继承 + 待收敛落子）

**已闭环（生产就绪，合 main 即上线）**：
- 三核心游戏化基线（R144–R149）：积分/闯关/奖励解锁/即时反馈/进度成就五项全覆盖；`@/game` 统一编排层（engine + useGamification + GameErrorBoundary + useReducedMotion）+ `components/gamification`（GentleFeedback / MistakeBookPanel / AchievementWall / LevelProgress / ComboMeter / RestReminder）生产级组件库已 build 并接线。
- 十维矩阵 `G(目标)+L(合规)+S(语音)+P(性能)+A(无障碍)+I(自适应)+R(留存)+N(AI安全)+M(音乐)+E(实证)` 方案层完整，验收标准含技术七门禁 + 六维增强 + 合规七项。

**待 WIP 收敛后落子（均 NEW 文件 / 加法挂载，与 R152 决策一致）**：
- A/S/P/I/N/M 深度接入三核心练习闭环（现以 `@/game` 组件库待命，用户合并 WIP 后增量 `import` 即可）。
- D 层 `!` 非空断言收敛（numbers 最弱，仅外围非核心热点，不回写三核心）。

**验收硬门禁（持续满足）**：① tsc EXIT=0 ② vite build EXIT=0 ③ 全量 vitest ≥1186 全绿 ④ lint 基线通过（仅 WIP 增量红，合 main 前零新增）⑤ 三核心五项硬指标命中 ⑥ 零回归（连续 17 轮）⑦ 外壳页机制命中 ≥12。

## 五、隔离与交付

- **隔离**：仅提交并推送本 R153 报告（1 file）；193 用户 WIP（含 App.tsx / 三核心主文件 / @/game 等活跃编辑）**零触碰**；lint 基线不刷新；未做破坏性重写、未合并分支、未触发部署。
- **部署就绪判定**：已提交 HEAD 满足 `tsc0 + build0 + lint基线通过 + vitest全绿`，合 `学习乐园`→`main` 即触发 `deploy-pages`，无需额外调整即可生产环境部署。
- **交付物**：`docs/三大模块游戏化升级守门复验_R153_2026-08-28.md`。

## 未决事项（跨轮持续）
1. 部署阻塞已解除（R122 闭环）：生产构建 `badgeSync`/`celebrateLarge` 悬空导入已还原；合 main 即 deploy-pages。
2. 超范围增强（待授权）：双路线 C / 识字表三态 / 错字本 mount / 先测后学 I1 / 自动跳过 I5 / 多孩档案 / L1–L4 分级 / S 语音反馈层 / P 性能层 / A 无障碍层 / N AI 安全 / M 音乐层——均以新文件/新入口落地，不触碰三核心学习逻辑。
3. 三核心改造范围（R147 修正）：研究期"不回写三核心"已转为"加法挂载·学习逻辑零改动"；剩余矩阵维度以 `@/game` 组件库增量采用，待用户 WIP 收敛后接线。
4. 全量 vitest 失败：当前为 `SpeedMath.test.tsx`（用户 WIP 快速连点锁，非三核心、非本轮引入），按隔离原则不代改，待用户收敛后独立处置。
