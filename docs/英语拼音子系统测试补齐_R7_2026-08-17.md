# 英语单词(words) / 拼音(pinyin) 子系统分批补测（R7）

> 日期：2026-08-17 ｜ 项目：宝贝学习乐园（React 19 + Vite 7 + TypeScript strict + Zustand）
> 定位：承接 R6《数学子系统测试补齐》之后的第二批补测，覆盖剩余两个子系统。

---

## 一、背景与目标

R6 完成数学(numbers)子系统第一批补测后，words（英语单词）/ pinyin（拼音）
两个子系统仍仅有页面级冒烟。本轮（R7）按 R5–R6 确立的
「**导出纯函数/数据常量 + 组件冒烟 + 关键交互**」低风险范式补齐两个子系统。

严格限定范围：只做安全重构（加 export）与测试新增，零行为变更、零新模块。

---

## 二、实施内容

### 1. 安全重构（仅加 export，行为等价）

| 文件 | 导出 | 覆盖的正确性逻辑 |
|---|---|---|
| `src/modules/words/BodyParts.tsx` | `PARTS` | 身体部位数据（12 个、区域分布） |
| `src/modules/pinyin/PinyinGroup.tsx` | `YUNMU` / `CATEGORIES` | **韵母四类分类**（教学正确性核心：单6/复9/前鼻5/后鼻4） |
| `src/modules/pinyin/Dictation.tsx` | `makeQuestion` | 听写出题（4 选项含正确答案且不重复） |

### 2. 新增 `src/modules/words/words.test.tsx`（16 用例）

| 分组 | 覆盖内容 |
|---|---|
| WORD_FAMILIES 数据契约（4） | 38 族总量、level 1/2/3 覆盖、id 唯一、pattern/sound/desc 非空、每族词 ≥3 且不重复、`getFamiliesOfWord` 大小写不敏感反查 |
| Phonics 发音规则（3） | 26 字母覆盖 a–z、组合规则唯一、全部规则 letter 无跨表冲突、examples 非空 |
| BodyParts（3） | 12 部位/区域分布 head5·torso3·limb4；渲染 12 按钮；点击显示描述；切「点一点」模式 |
| WordFamilyGame（3） | 默认渲染词族卡片与数据一致；点击进入练习页显示目标词 + 4 选项；答对回写 `word:family:at` 且显示正确反馈 |
| PhonicsListen（1） | 开始界面冒烟（不挂载重量级 Runner） |

### 3. 新增 `src/modules/pinyin/pinyin.test.tsx`（11 用例）

| 分组 | 覆盖内容 |
|---|---|
| YUNMU 数据契约（4） | 24 个、单6/复9/前鼻5/后鼻4、唯一性、前鼻 n 尾/后鼻 ng 尾、CATEGORIES 完整性 |
| makeQuestion 出题纯逻辑（2） | 4 选项含正确答案且不重复；20 次调用存在随机性 |
| Dictation 冒烟（1） | 1/20 进度、重听按钮、4 个拼音选项渲染 |
| PinyinPractice（1） | 组合表条目数与数据一致；点击后出现练习面板（听/讲解按钮） |
| PinyinGroup 交互（3） | 8 个待分类 + 4 分类框；**正确归类**回写 `pinyin:group:<type>,true,1` 并显示反馈（fake timers 推进重置）；**错误归类**回写 false 并显示纠错反馈 |

---

## 三、验收结果（全量质量门禁全绿）

| 门禁 | 结果 |
|---|---|
| `npx tsc -b` | ✅ 0 error |
| `npx vitest run` | ✅ **626/626 通过**（44 文件；R6 599 → R7 626，+27） |
| `npm run lint:budget` | ✅ 通过（no-non-null-assertion 454，与 R6 持平，零新增） |
| `npx vite build` | ✅ 构建成功（57.6s；chunk 体积提示为既有 three/opencc vendor，非本轮引入） |

**无回归**：全量 44 个测试文件全部通过，含 R1–R6 累积回归套件。

---

## 四、范围与后续（保持严格范围）

- ✅ 本轮完成：words / pinyin 两个子系统的数据契约 + 纯逻辑 + 组件冒烟/交互补测。
- ⏳ 留待后续（超出本轮范围）：
  1. **体积优化**：`vendor-three`(972KB) / `vendor-opencc`(1180KB) chunk 超 900KB，可评估动态 import 拆分（既有提示，非本轮引入）；
  2. 若需进一步，可对 words/pinyin 的重交互组件（WordLearn/WordMatch/TonePractice/BlendPractice）做第二轮冒烟——依赖面较重，建议按需单独立批。
- 改动未 git 提交（依用户约定不主动 push）。

---

## 五、交付物清单

| 文件 | 说明 |
|---|---|
| `src/modules/words/BodyParts.tsx` | 导出 PARTS（+export 前缀，零行为变更） |
| `src/modules/pinyin/PinyinGroup.tsx` | 导出 YUNMU / CATEGORIES（+export 前缀，零行为变更） |
| `src/modules/pinyin/Dictation.tsx` | 导出 makeQuestion（+export 前缀，零行为变更） |
| `src/modules/words/words.test.tsx` | 新增，16 用例 |
| `src/modules/pinyin/pinyin.test.tsx` | 新增，11 用例 |
| `docs/英语拼音子系统测试补齐_R7_2026-08-17.md` | 本报告 |
