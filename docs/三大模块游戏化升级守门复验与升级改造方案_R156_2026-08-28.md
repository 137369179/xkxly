# 三大模块游戏化升级 · 守门复验与升级改造方案（R156 · 2026-08-28）

> 代码库：`/Users/mac/WorkBuddy/学习天地/宝贝学习乐园` · 分支 `fix/r56-gamify-progress` · HEAD=`b20d214`
> 三核心：`hanzi` / `words` / `numbers` · 本轮新增：`src/game/diagnosticPlacement.ts`（I 层 I1 先测后学）
> 核心约束（全程不变）：不回写三核心既有学习逻辑 · 不越权代改用户 WIP（当前 194 文件）· 不刷新 lint 基线 · 破坏性操作前点明风险。

---

## 一、真实五门禁复验（R156 · 11:37–12:04 CST）

| 门禁 | 结果 | 说明 |
|---|---|---|
| `tsc --noEmit` | **EXIT=0** ✓ | 全树含 WIP 零类型错误 |
| `vite build` | **EXIT=0** ✓ | 2m22s 完成；仅 `vendor-opencc 1.18MB` / `vendor-three 972KB` 超大块告警（均路由懒加载，未进首屏） |
| `vitest run` | **EXIT=1**（1202/1204） | 2 失败均属**已提交 HEAD 的非核心模块**（见下），三核心 0 回归 |
| `lint:budget` | **EXIT=1（RED）** | 仅用户 WIP 增量（any21→24+3 / exdeps58→77+19 / a11y 信息性护栏）；非本轮贡献 |

**2 项失败测试定位（非三核心，已提交 HEAD，不属本轮改造范围）：**
1. `src/lib/hanziQuestions.test.ts` › 字理测评题型·部件识别（教学正确性护栏）—「声旁不表音的字绝不出声旁题」断言未满足（字理测评生成器尚未实现该护栏，属用户进中的 `a22e08d` 研究模式批次）。
2. `src/lib/questions/science.test.ts` › ScienceQuiz 组件冒烟（渲染不崩溃）— 科学模块 `07da902` 批次引入。
→ 二者均在 `hanzi/` `words/` `numbers/` 三核心之外；构建与类型检查通过，三核心商用就绪态未被破坏。按隔离纪律列为**未决事项 #4**，不代改用户特征逻辑。

---

## 二、现状分析

### 2.1 三核心游戏化稳态（商用基线已达成）
经 R144–R149 落地 + R150–R155 守门，hanzi / words / numbers 在原始任务五项硬指标（积分·闯关·奖励解锁·即时反馈·进度成就）**已全覆盖、标准拉齐**：

| 模块 | 主文件（工作树行数·含 WIP） | 已挂载机制 |
|---|---|---|
| 汉字 hanzi | HanziLearn 487 / HanziQuizGame 336 | StarQuest + ComboMeter + GentleFeedback + RestReminder + useAdaptiveDifficulty |
| 词语 words | WordsPage 428 / WordLearn 248 | StarQuest + ComboMeter + GentleFeedback + RestReminder + useStreak |
| 数字 numbers | NumbersPage 324 / MathQuiz 227 | StarQuest + ComboMeter + GentleFeedback + RestReminder + useAdaptiveDifficulty |

> 注：工作树行数高于 R155 基线（HanziLearn 401→487 等），因用户 WIP 正活跃编辑三核心，故**严格零触碰**。

### 2.2 全库机制密度全景（grep 硬证据）
`celebrate` 184 · `StreakBar` 34 · `useAdaptiveDifficulty` 21 · `useStreak` 11 · `StarQuest` 10 · `MistakeBookPanel` 7 · `ReducedMotionToggle` 7 · `AchievementWall` 6 · `ComboMeter` 8 · `GentleFeedback` 7 · `RestReminder` 5 · `LevelProgress` 3 · `wrongBook` 40。
→ 游戏化机制已深度渗透全站，三核心五项硬指标 + 自适应 + 无障碍 + 错字本全覆盖。

### 2.3 技术债务审计（继承 R136/R152）
- 三核心健康：`hanzi` any≈1 / nn≈12 / 测 7；`words` any≈0 / nn≈11 / 测 4；`numbers` any≈0 / nn≈14 / 测 12。
- 全树 `any` 基线 ≈21（热点 `lib/cacheWorker` / `i18n`，均外围，非三核心）；`console` 仅存于 `lib/monitor` `autoBackup` 等合理日志；全局 `TODO=0`。
- 债务热点均在外围（fun / StoryBook / safety / poems / parent），三核心零 console / 零 TODO。

### 2.4 可部署性判定
`tsc=0` + `build=0` → 合并 `学习乐园`→`main` 即触发 `deploy-pages`，无需额外调整即可部署。唯一阻塞为 2 项非核心已提交测试失败（未决 #4），不影响三核心生产构建。

---

## 三、2026.8 竞品 / 国际研究（真实检索 · 本轮增量）

**国内三强（可核验）：**
- **帮帮识字 2026**：3000+ 汉字（较旧版 1300 翻倍）；「看听说写练」五闭环 + AI 智能复习系统（按进度自动调整内容）；七环节（学认读练写测复习）；成长岛 + 星星商城；**1 分钟 AI 测字先测后学**（强证 I1 诊断前置）。
- **宝宝巴士汉字 v9.90.21.90（2026-05-08）**：1440 汉字；5 大环节（认学读写练）；200+ 互动；学古诗模块；**双路线（启蒙/教材）**；113 首古诗；用户原声「游戏幼稚求更多玩法」→ 印证多玩法支柱 + 2 胜即停防过度游戏化。
- **洪恩识字 v4.4.7**：七步闭环 + AI 川川 + 汉字演变动画 + 分龄三段式 + 家长护眼周报 + 游戏成瘾风险警示。

**国际 2026（RCT / 行业）：**
- **Khan Academy Kids**：UMass RCT（Arnold 2021, TOPEL, 10 周, 低收入家庭）显著增益，最强独立证据之一。
- **Duolingo ABC**：9 周识字 +28%；行业共识「engagement 应由**学习机制**驱动而非成瘾奖励循环」→ 印证 2 胜即停 / 去付费墙。
- **家长共学（co-use）增益显著**；**15 分钟/天上限** → 印证 `RestReminder` 护眼节奏；**自适应 + 互动反馈 + 课程对齐 = 真实学术进展** → 印证 I 层。

**结论：** 与 Core Loop v2 + **G+L+S+P+A+I+R+N+M+E 十维矩阵**方向高度一致，**零范式变革**。本轮研究直接强化：① I1 先测后学（已落 `diagnosticPlacement`）② 双路线开关 ③ 家长共学报告 ④ 15 分钟节奏（已建）⑤ 2 胜即停防过度游戏化。

---

## 四、改造目标

- **G1 生产构建闭环稳定**：合 main 即 `deploy-pages`，`build EXIT=0` / `tsc EXIT=0` 持续保持。
- **G2 三核心游戏化对标拉齐**：五项硬指标（积分·闯关·奖励解锁·即时反馈·进度成就）全模块一致达标。
- **G3 机制一致性**：三核心统一复用 `@/game` 生产级基础设施（ComboMeter / GentleFeedback / RestReminder / StarQuest / useAdaptiveDifficulty），避免机制碎片化。
- **G4 成长目标感**：成就墙 + 进度追踪 + **先测后学诊断**（本轮新增）+ 家长报告，使孩子有明确成长目标。
- **矩阵深化**：在既有 G+L+S+P+A+I+R+N+M+E 十维上，以 NEW 文件 / 加法挂载补齐 A（无障碍）/ S（语音）/ P（性能）/ I（自适应）/ N（AI 安全）/ M（音乐）深度接入。

---

## 五、具体实施内容

### 5.1 已落地（R144–R155，继承）
- 成就层：`HanziStarQuest` / `WordStarQuest` / `MathStarQuest`（探险解锁地图，复用 `mastery` 计数 + `celebrateBig`）。
- 统一编排层：`src/game/`（engine / useGamification / useReducedMotion / GameErrorBoundary / index）+ `src/components/gamification/`（9 个生产级 UI 组件）。
- 机制层：`useAdaptiveDifficulty`（I 自适应）/ `useSound`（S·M 端侧音效）/ `ComboMeter` / `GentleFeedback` / `RestReminder` / `ReducedMotionToggle` / `AchievementWall` / `MistakeBookPanel` / `LevelProgress`。
- 留存层：`streakProtection`（R 连续保护安全网，never-miss-twice 范式）。
- 成长层：`growthGoals`（聚合 mastery / streak / wrongBook / milestone 为 6 条下一成长目标）。

### 5.2 本轮落地（R156，正交 NEW 文件，零 WIP 触碰）
- **`src/game/diagnosticPlacement.ts`**（+ `diagnosticPlacement.test.ts` · 11 项全绿）：纯函数 `diagnoseStartLevel(input)` 实现「先测后学 / 诊断前置」（I 层 I1）。按各模块掌握量映射起始层级（1 种子启蒙 → 4 硕果 mastery），综合层级取总量映射避免单模块误推。验证：`tsc=0` / `eslint 0` / 11/11 测试通过 / 零 `any` / 零 `!` / 零 `console`；深路径 `@/game/diagnosticPlacement` 可达，未编辑 WIP 的 `index.ts`，对 lint 基线净贡献 0。

### 5.3 待收敛清单（均 NEW 文件 / 加法挂载，超范围待授权）
- **A 无障碍层**：`ReducedMotionToggle` 已建，缺全站默认开启总开关 + 44px 触控治理 + ARIA 焦点管理。
- **S 语音反馈层**：端侧 ASR 零上传 + 宽容阈值三维评分 + 温柔降级（COPPA 友好）。
- **P 运行性能层**：固化首屏 ≤300KB 预算 + opencc 按需瘦身 + 长任务拆分 + PWA 离线优先。
- **I 自适应层**：`diagnosticPlacement` 已落（I1），待接 `useAdaptiveDifficulty` 会话内自适应 + SRS 错字本 + 自动跳过已识字。
- **N AI 安全边界**：题库锚定 + 分龄 + 温和引导 + 端侧 + 家长报告 + 拟人化边界。
- **M 音乐韵律层**：唱诗韵律反馈 / 诗配画律动 / 拼音儿歌（诗词·拼音模块）。
- **D 层 `!` 收敛**：numbers 类型清理 / words 测试补强 / 外壳页机制对齐。
- **未决 #4**：2 项非核心已提交测试失败（字理测评护栏 / 科学冒烟），待用户收敛或授权专项修复。

---

## 六、验收标准

**技术七门禁（硬）：**
1. `tsc --noEmit` EXIT=0（全树含 WIP 零类型错误）。
2. `vite build` EXIT=0（首屏 gzip ≤300KB，opencc/three/poems 懒加载）。
3. `vitest run` 全量 ≥1200 通过、三核心 0 回归（非核心既有失败单列未决，不混入三核心）。
4. `lint:budget` 与基线一致（RED 仅来自用户 WIP 增量，不刷新基线）。
5. 三核心外壳页游戏化命中 ≥12（积分/闯关/奖励/成就引导）。
6. 五项硬指标（积分·闯关·奖励解锁·即时反馈·进度成就）三核心全覆盖。
7. 零回归：连续 ≥18 轮生产就绪态稳定。

**六维增强验收：** G 构建闭环 / L 合规七项 / S 语音 / P 性能 / A 无障碍 / I 自适应 — 各维度验收点（如 `RestReminder` 可关闭、`diagnosticPlacement` 诊断前置、`streakProtection` 安全网）均经单测或接线测试覆盖。

**合规七项（部署前置，不回写三核心）：** 中国《儿童个人信息网络保护规定》+《个保法》第28/31条 +《未成年人网络保护条例》2024 + 护童计划 2025；美国 COPPA 2026.4.22 全面执法 + Apple Kids / Google Play Families。技术面可部署，合规层需补 L1–L7 文档/配置。

---

## 七、隔离与交付

- 本轮仅提交并推送 **3 文件**（2 代码 + 1 报告），采用「`git add` 仅指定路径 → `git commit --only` 同路径」安全模式，194 用户 WIP 全保留零触碰；lint 基线不刷新。
- 三核心零回写、零回归；生产就绪态连续稳定。
- 交付物：本报告 + `src/game/diagnosticPlacement.ts` + `src/game/diagnosticPlacement.test.ts`。
