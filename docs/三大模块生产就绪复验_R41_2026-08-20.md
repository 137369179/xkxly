# 三大核心模块生产就绪复验 · R41

**执行时间**：2026-08-20 21:16–21:23
**代码库**：`/Users/mac/WorkBuddy/学习天地/宝贝学习乐园`
**分支**：`学习乐园`（复验起点 HEAD = `d259665`）
**轮次性质**：R1–R40 全链路早已闭环（识字 / 英语 / 数学三模块已达商用标准并上线）。本轮为守门复验 + 主动 push，**零源码回写**。

---

## 一、结论

**门禁执行窗口（21:16–21:23）四门禁真实执行全绿**，三大核心模块持续满足可商用部署标准，线上站点在线。

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| 类型检查 | `npx tsc -b --force` | ✅ **0 error**（2m01s，全量强制重建） |
| Lint 预算 | `npm run lint:budget` | ✅ 通过（warning **519**，无规则超基线） |
| 单元测试 | `npx vitest run` | ✅ **728 / 728**（55 文件，62.15s） |
| 生产构建 | `npx vite build` | ✅ **BUILD_EXIT=0**（1m06s） |

> ⚠️ **时效波动说明（已闭环）**：门禁通过后的 21:22–21:23，外部并行会话向共享文件落盘在编中间态改动，一度引入 **3 个类型错误**。经定向归因，错误 100% 属外部 WIP（成语模块 + 共享 store 中间态），**三核心模块文件零报错、运行时零回归**（171/171 全绿）。21:27 外部提交 `c3c8b7f` 补齐功能后，**`tsc -b` 复测已恢复 0 error**。全过程详见 §5.3 及其闭环追记 §5.3.1。本轮依 R27/R28 惯例全程未代改外部在编代码，该决策经 §5.3.1 反向验证为正确。

**Lint 基线持续优于门禁值**：`no-non-null-assertion` 524→441（−83）、`exhaustive-deps` 58→51（−7）、`no-explicit-any` 22→21（−1）。

**测试面较 R40 扩大**：728（本轮）vs 722（R40），+6 用例来自成语模块外部新增测试，非本轮改写产生。

---

## 二、三大核心模块专项核验

### 2.1 测试

| 范围 | 结果 |
| --- | --- |
| 模块目录定向（hanzi / letters / words / numbers / logic） | ✅ **45 / 45**（5 文件，14.68s） |
| 完整测试面（含 SRS 契约与 hanzi 数据层） | ✅ **234 / 234**（14 文件，19.00s） |

完整测试面覆盖 14 个文件，**超出 R40 记录的 148/148（8 文件）基线覆盖范围**，纳入：

- 契约层：`store/closure.test.ts`、`lib/skillRouting.test.ts`、`lib/wrongCluster.test.ts`、`lib/srs.test.ts`
- 交互层：`modules/hanzi/WordBuilder.test.tsx`、`modules/hanzi/hanziEtymologyViz.test.tsx`、`modules/letters/LetterTrace.test.tsx`、`modules/words/words.test.tsx`、`modules/numbers/numbers.test.tsx`
- 数据层：`lib/hanziEtymology.test.ts`、`lib/hanziQuestions.test.ts`、`lib/hanziWriting.test.ts`、`data/hanziSentences.test.ts`、`components/QuizCard.hanzi.test.tsx`

### 2.2 源码稳定性

源文件计数与历史基线完全一致（合计 68）：

| 模块 | 文件数 |
| --- | --- |
| hanzi（识字） | 22 |
| letters（英语字母） | 9 |
| words（英语词汇） | 13 |
| numbers（数学） | 20 |
| logic（数学逻辑） | 4 |

`git status -s` 对五个核心目录输出为**空**——三核心模块自 R24 合入 `origin/main` 起**零改动**，本轮亦未回写任何一行。

---

## 三、构建产物与首屏预算

| 产物 | 原始 | gzip |
| --- | --- | --- |
| `index-CuEHTg-o.js`（首屏入口） | 491.27 kB | **181.43 kB** |
| `vendor-react` | 192.32 kB | 60.25 kB |
| `data-hanzi` | 187.02 kB | 54.17 kB |
| `vendor-pinyin` | 311.50 kB | 141.77 kB |
| `data-poems` | 787.13 kB | 199.26 kB（懒加载） |
| `vendor-three` | 972.02 kB | 266.97 kB（懒加载） |
| `vendor-opencc` | 1,180.03 kB | 506.16 kB（懒加载） |

- 首屏 gzip **181.43 kB**，较 R40 的 181.36 kB 仅 +0.07 kB（成语模块外部改动所致，在噪声范围内）。
- `three` / `opencc` / `poems` 三个大库**仍全部懒加载、不进首屏**，R8 的 `modulePreload` 精准过滤策略持续生效。
- 唯一告警为既有 `chunk > 900 kB` 提示，属**非阻断**性质，且相关 chunk 均不在首屏关键路径。

---

## 四、生产在线验证

```
curl https://137369179.github.io/xkxly/
→ HTTP 200 ｜ 4261 B ｜ 1.50 s
```

---

## 五、本轮两项关键发现

### 5.1 R40 遗留的批量合并事项已闭环

R40 记录「`学习乐园` 领先 `origin/main` 7 个纯文档提交（R34–R40），留待批量并入 main」。本轮实测：

```
git rev-list --left-right --count origin/main...HEAD  →  0    0
git branch -r --contains d259665                      →  origin/main
```

R34–R40 的 7 个文档提交连同成语 SRS 提交 `d259665` **均已合入 `origin/main`**，两者无分叉。该遗留事项无需再跟踪。

### 5.2 TS6053 为外部 WIP 竞态，非代码缺陷

首次 `tsc -b` 报出：

```
error TS6053: File 'src/modules/idioms/_perf.demo.test.tsx' not found.
  Matched by include pattern 'src' in 'tsconfig.app.json'
```

**根因**：外部并行会话于 21:16 在 `src/modules/idioms/` 创建临时性能 demo 测试文件（当时 `git status` 记为 `??`），tsc 启动时经 `include: src` glob 匹配到该文件，但文件在 21:17 被外部删除，编译器解析阶段已无法读取 → TS6053。

**证据**：复查目录（mtime 21:17）确认 `_perf.demo.test.tsx` 已不存在，`git status -s src/modules/idioms/` 仅余 `M IdiomReviewCenter.tsx`。

**处置**：依 R33「门禁结论过期即重跑」原则，在目录稳定后执行 `npx tsc -b --force` 全量强制重建 → **0 error**。判定为外部 WIP 生命周期竞态，与三核心模块及既有代码质量无关，无需任何修复。

> 附记：本轮首次跑 vitest 时误传 `--reporter=basic`（该 reporter 已被当前 vitest 版本移除）导致 Startup Error，属执行侧参数失误，改用默认 reporter 后 728/728 全绿。记录于此以免后续轮次误判为测试回归。

### 5.3 外部 WIP 在编中间态引入 3 个类型错误（已定向归因 · 不代改）

门禁全绿后，外部会话于 **21:22–21:23** 继续向共享文件落盘改动：

| 文件 | 变更 | 落盘时间 |
| --- | --- | --- |
| `src/types.ts` | `+2 / −0` | 21:22 |
| `src/store/storeSlices/learningSlice.ts` | `+14 / −0` | 21:23 |

因这两个文件为**三核心模块复用的共享基础设施**，不能沿用可能过期的门禁结论放行，故对新状态**重跑类型检查**，实测 3 个错误：

```
src/modules/idioms/IdiomReviewCenter.tsx(32,49): error TS2339:
  Property 'completeDailyReview' does not exist on type 'StoreState'.
src/store/storeSlices/learningSlice.ts(17,5): error TS2344:
  Type '"completeDailyReview"' is not assignable to type 'keyof StoreState'.
src/store/storeSlices/learningSlice.ts(42,25): error TS7006:
  Parameter 'stars' implicitly has an 'any' type.
```

**根因判定**：外部会话在 `learningSlice.ts` 新增了 `completeDailyReview` action 并在 `IdiomReviewCenter.tsx` 调用，但尚未在 `StoreState` 类型定义中补齐对应声明，且新增回调参数 `stars` 缺少类型标注——典型的**功能在编中间态**，非既有代码缺陷。

**影响面定向核验**：

| 核验项 | 结果 |
| --- | --- |
| 报错文件归属 | 仅 `modules/idioms/IdiomReviewCenter.tsx`、`store/storeSlices/learningSlice.ts` |
| 三核心模块文件报错数 | **0** |
| 三核心 + 契约层运行时回归 | ✅ **171 / 171**（9 文件，6.84s） |

**处置决策**：依 R27 / R28 惯例，外部在编 WIP **不代改、不提交，仅监控告警**。理由有三：① 错误全部落在成语模块与其配套 store 中间态，超出「识字 / 英语 / 数学」既定改造范围；② 外部会话仍在活跃编辑（文件 mtime 持续推进），代改必然产生冲突并覆盖他人在编成果；③ 三核心模块运行时经实测零回归，生产已部署版本不受影响。

**给外部负责会话的修复提示**（供参考，本轮未执行）：在 `StoreState` 类型中补充 `completeDailyReview` 的方法签名，并为 `learningSlice.ts:42` 的 `stars` 参数补类型标注，即可消除全部 3 个错误。

#### 5.3.1 闭环追记（21:27–21:28 · 事后实测）

本报告首次落盘后数十秒，外部会话提交 `c3c8b7f`「feat: add daily idiom review reward feature」，自行补齐了 `reviewDate` 进度字段、`completeDailyReview` store 方法与配套多语言文案。**对该提交后的状态重跑类型检查**：

```
npx tsc -b  →  TSC_EXIT=0 ｜ 错误数 0
```

§5.3 记录的 3 个类型错误**已由外部负责会话自行闭环消解**，磁盘恢复 0 error。

此结果反向验证了本轮「不代改外部在编 WIP」决策的正确性——该中间态本就是外部功能的未完成切片，若本轮擅自补写 `StoreState` 声明，必然与 `c3c8b7f` 产生冲突并覆盖其正式实现。**R27 / R28 的「监控告警而非代改」惯例，在本轮获得一次真实有效性验证。**

#### 5.3.2 并行会话提交竞态（协作风险记录）

同一时间窗内另有现象：外部提交 `ecccede`（21:27:02）「docs: add R41 production readiness review report」纳入了**本报告的 144 行早期草稿**——该草稿当时正处于本会话暂存区（已 `git add` 但尚未 `commit`），被外部会话的全量暂存操作（`git add -A` 类）一并提交。

本会话随后的 `060810f`（21:27:32）提交 183 行完整版（补入 §1 时效说明与 §5.3 全节），构成正当版本递进，**未覆盖任何外部原创内容**——经 `git show ecccede:<path>` 逐节比对确认：该版本章节结构与本会话草稿完全一致，仅缺后续补写部分。

**风险提示**：多会话并行操作同一工作树时，`git add -A` / `git commit -a` 会连带提交其他会话暂存区中的在编文件。建议后续各会话统一采用**精确路径 `git add`**（本会话自 R27 起即遵循此约束），以避免交叉污染与意外覆盖。

---

## 六、R40 后磁盘变动盘点（外部在编 · 全未触碰）

| 文件 | 状态 | 归属 | 处置 |
| --- | --- | --- | --- |
| `public/icons/*.png`（7 个） | `M` | PWA 图标统一资产 | 未触碰、未混入提交 |
| `src/modules/idioms/IdiomReviewCenter.tsx` | `M` | 成语模块（超出三核心范围） | 未触碰、未混入提交 |
| `src/modules/idioms/_perf.demo.test.tsx` | `??` → 已删除 | 外部临时 demo | 未触碰（生命周期竞态见 §5.2） |
| `src/types.ts` | `M`（+2） | 共享类型定义（在编中间态） | 未触碰（类型错误见 §5.3） |
| `src/store/storeSlices/learningSlice.ts` | `M`（+14） | 共享 store（在编中间态） | 未触碰（类型错误见 §5.3） |

三核心模块目录 `git status -s` = **空**（两次核验一致）。外部改动分两批：前批（图标 + `IdiomReviewCenter`）经全量 `tsc` + `vitest` 验证无阻断；后批（`types.ts` + `learningSlice.ts`，21:22–21:23 落盘）引入 3 个类型错误，已定向归因为外部在编中间态，三核心运行时零回归。两批均按 R27 / R28 惯例仅监控告警，**不代改、不提交**。

---

## 七、范围合规声明

- **零源码回写**：本轮仅执行读取、四门禁复验、报告撰写、单文件提交与推送。
- **未对三核心模块重复改写**：识字 / 英语 / 数学自 R24 合入主线起保持零改动。
- **未引入无关新模块或冗余功能**。
- **未混入外部在编 WIP**：`git add` 精确到报告单文件，排除 7 个图标与成语模块改动。

---

## 八、后续轮次指引

1. 继续守门复验，**严禁重复回写三核心模块**。
2. §5.3 的 3 个类型错误**已于本轮内由外部 `c3c8b7f` 闭环**（`tsc -b` 复测 0 error），下轮无需跟踪；但因成语模块仍在活跃开发，建议**下轮开局仍先跑一次 `npx tsc -b`** 确认磁盘态。遇同类在编中间态错误，沿用本轮范式处理：定向归因 → 核验三核心是否受累 → 记录告警 → **不代改**。
3. 持续监控外部在编改动（图标 / PWA / 成语 / 宠物 / UI / AI 语音 / `srs.ts` / `learningSlice.ts` / `types.ts` 共享核心）；若引入回归，同样按 R14 最小桥接处理。
4. 遇 TS6053 一类「文件 not found」错误，先判定是否为外部 WIP 竞态（复查目录 + `git status`），确认后 `tsc -b --force` 重跑，不要误改 `tsconfig` 的 `include`。
5. 建 PR / 合入统一走 R33 的 `osxkeychain` classic PAT + REST API 通道，不依赖已损坏的 `gh` CLI 或只读 MCP。
6. 超范围增强须用户确认：① `index` 进一步拆 vendor 瘦身；② 扩展测试面；③ `dist/` 历史 stale `index-*.js` 清理（可加 `emptyOutDir`）。
