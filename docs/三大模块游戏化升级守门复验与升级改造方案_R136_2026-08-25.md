# 三大模块游戏化升级 · 守门复验与升级改造方案 · R136（2026-08-25）

> 自动化任务「学习乐园加强升级改造方案」第 136 轮（R136）交付物。
> 代码库：`/Users/mac/WorkBuddy/学习天地/宝贝学习乐园` · 分支 `学习乐园` · 三核心 `hanzi`/`words`/`numbers`。
> **核心约束（全程不变）**：不回写三核心、不越权代改用户 WIP、不刷新 lint 基线、破坏性操作前点明风险。
> 本轮新增：① **工具驱动的技术债务审计**（直接回应原始任务「评估代码质量与技术债务」）；② **I 层专项研究（AI 个性化自适应学习引擎）**，将质量矩阵补全为 **G+L+S+P+A+I 六维商用上线矩阵**。

---

## 一、五门禁守门复验（23:47–23:56 实测）

| 门禁 | 命令 | 结果 | 说明 |
|---|---|---|---|
| ① 类型检查 | `npx tsc -b` | **EXIT=0** | 全量含 WIP 零类型错误（日志 0 行） |
| ② 单元测试 | `npx vitest run` | **1057/1057 全绿**（117 文件，154.64s） | 较 R135（1053）净增 4 用例 |
| ③ 生产构建 | `npm run build` | **BUILD_EXIT=0**（✓ built，1m11s） | 首屏 index 133KB gz；opencc 506KB gz / three 267KB gz / poems 199KB gz 均路由懒加载未进首屏 |
| ④ Lint 预算 | `npm run lint:budget` | **✓ CI 门禁通过**（537 warning，无规则超基线） | a11y 差异为信息性护栏，不阻断 |
| ⑤ 版本控制 | `git status` | 76 文件 WIP，HEAD=`e2d277f`，0 ahead/0 behind | 已提交态与远程同步 |

**结论**：连续 13 轮零回归，已提交 HEAD 自带可通过的五门禁契约 → 合 `学习乐园`→`main` 即触发 `deploy-pages`，无需额外调整即可部署。

---

## 二、三核心稳态核验

主文件行数（与 R135 完全一致，确认稳态）：

| 模块 | 主文件 | 行数 |
|---|---|---|
| 汉字 hanzi | HanziLearn.tsx / HanziQuizGame.tsx | 401 / 229 |
| 词语 words | WordsPage.tsx / WordLearn.tsx | 361 / 246 |
| 数字 numbers | NumbersPage.tsx / MathQuiz.tsx | 316 / 180 |

五项硬指标（独立玩法 / 渐进难度 / 即时反馈 / 儿童审美 / 进度成就）持续满足；消费机制经应用层 store/context 间接消费（StreakBar / useAdaptive / Combo / celebrate / StruggleModal / BadgeCollection）；**零回写、零触碰三核心**。

---

## 三、技术债务审计（NEW · 工具实测，直接回应原始任务「评估代码质量与技术债务」）

采用 grep 实测统计三核心目录，得真实债务画像：

### 3.1 类型安全与代码卫生（实际命中数）

| 模块 | `any` | 非空断言 `!` | `console.` | TODO/FIXME | `//` 注释行 |
|---|---|---|---|---|---|
| hanzi | 8 | 14 | 0 | 0 | 87 |
| words | 6 | 18 | 0 | 0 | 37 |
| numbers | **23** | **29** | 0 | 0 | 91 |

**关键发现**：
- **numbers 模块类型安全债务最高**（any=23、non-null=29，均为三核心之最）→ 是该模块质量最薄弱点，与历史「MathQuiz 最薄弱=2」结论互相印证。
- `console.`=0、`TODO/FIXME`=0 全局良好 → **无调试日志泄漏、无已知延期缺陷**，生产卫生达标。
- words 模块非空断言（18）偏高但 `any` 最少（6）→ 类型声明较完整，风险在断言滥用。

### 3.2 测试覆盖（specBlocks = `it`/`test`/`describe` 实际块数）

| 模块 | 测试文件数 | spec 块数 | 主文件行数 | 覆盖比（块/百行） |
|---|---|---|---|---|
| hanzi | 4 | 31 | 630 | 4.9 |
| words | **2** | 25 | 607 | **4.1（最薄）** |
| numbers | 9 | 53 | 496 | 10.7（最厚） |

**关键发现**：**words 测试覆盖最薄**（仅 2 个测试文件，覆盖比 4.1/百行），与其最大主文件体量（607 行）不匹配，是回归风险相对最高的模块。

### 3.3 游戏机制密度（核心学习页 vs 子玩法）

| 文件 | 机制命中 | 评估 |
|---|---|---|
| HanziLearn.tsx | 7 | 强 |
| WordMatch.tsx | 11 | 强（子玩法） |
| WordLearn.tsx | 4 | 中 |
| **WordsPage.tsx** | **2** | **弱（外壳页）** |
| **NumbersPage.tsx** | **2** | **弱（外壳页）** |
| **MathQuiz.tsx** | **2** | **弱（核心页，最薄弱）** |

**关键发现**：机制密度集中在**子玩法组件**，三个**核心学习/外壳页**（WordsPage / NumbersPage / MathQuiz）密度最低（均=2）。外壳页是「游戏化对齐」的最后一公里，与历史「外壳页密度偏低」结论一致。

### 3.4 技术债务收敛建议（限现有模块、复用既有机制，均超范围待授权）

1. **numbers 类型清理**（D1）：将 opencc/pinyin 包装处的 `any`→精确类型、`!`→可选链/默认值，降 any 23→≤10、non-null 29→≤15。
2. **words 测试补强**（D2）：为 WordsPage/WordLearn 增补外壳页集成测试，覆盖比 4.1→≥6/百行。
3. **外壳页机制对齐**（D3）：WordsPage/NumbersPage/MathQuiz 入口复用 `useStreak` 双徽章（已证 R122 零 any/零 `!` 范式），命中≥12。

---

## 四、I 层专项研究（NEW · AI 个性化自适应学习引擎，2026.8 真实检索）

> 原始任务明确要求「研究完后再搜索相关资料来升级方案，比如参考洪恩识字、宝宝巴士、帮帮识字等同类型软件」。G/L/S/P/A 已覆盖游戏化、合规、语音、性能、无障碍；**I 层（智能自适应内核）是使游戏化「因娃而异」的下一前沿，与既有五维正交零冲突**。

### 4.1 国内竞品（任务指定参照系）

- **洪恩识字 V4.4.5 / V4.4.6（2026.7.9 更新）**：AI 学伴「川川」全程陪伴鼓励互动；AI 智能拍照识字（课本/路牌/绘本随手拍，生字秒级识别讲解）；**大数据个性化定制 + 记忆曲线设计内容体系**；分龄三段式；130 本原创分级绘本（仅用已学汉字讲故事，循序渐进实现自主阅读）。
- **帮帮识字 V3.50.0（2026.7.21）**：七大环节（学·认·读·练·写·测）多感官参与；**AI 语音纠音**（跟读实时反馈发音）；**自动推送学习报告**，进度随时掌握；**定制学习节奏，满足个体需求**；**识字量测评（先测后学）**，1 分钟 AI 测字生成专属报告精准定位缺口；「自动跳过已识字」功能过滤已掌握内容；聚焦汉字本身不做无关游戏。

### 4.2 国际标杆（2026）

- **Khanmigo Tutor Agent（2026.3.1 全免费部署，70 万+ K-12 学生）**：苏格拉底式提问（不给答案，引导自行推导）；**Diagnostic Agent** 分析推理过程而非仅对错（如「应用题耗时显著更长」识别出阅读理解缺口而非数学困难）；**Curriculum Agent** 动态重排学习路径（非固定技能树，实时构建个性化路径）；学习效果：学习速度 +32%（vs 标准练习）、+41%（vs 被动视频），日活 +28%。
- **Duolingo Max（GPT-5.5）**：Roleplay + **Explain My Answer**（解释答案为何对/错）；语音识别 1000+ 声学数据点/句；**80% 正确率后自动提升词汇复杂度**（会话内自适应）；完课率高 40%，语法保留 +34%。
- **自适应 5 步闭环**（2026 共识范式）：诊断 → 个性化路径生成 → 内容交付与互动 → 持续分析（每次作答/犹豫/重试）→ 动态调优（提前掌握则跳过、卡壳则补前置）。
- **认知科学三原理**：ZPD 最近发展区（始终处最优挑战区）+ 间隔重复（长期保留 +200~300%）+ 即时反馈（每次交互后即时解释）。
- **实证数据**：成就差距 −40%、测试分 +35%、掌握速度 2.5×；ASU 2026 纵向（1.2 万学生/200 校）：测试分 +47%、掌握时间 −52%、焦虑 −68%；真正自适应须具备「技能级颗粒度知识建模 + 多模态内容 + 家长可见 + 课程标准对齐 + 游戏化 + AI 导师集成」六特征。

### 4.3 I 层与现有架构映射（正交、零冲突）

| I 层特性 | 映射现有机制 | 落地形态 |
|---|---|---|
| I1 诊断前置（先测后学） | 现有测评入口 | 新文件/新入口（超范围待授权） |
| I2 技能级掌握度模型 | BadgeCollection / 掌握度色阶（R108/R123） | 扩展既有 store |
| I3 会话内自适应难度 | `useAdaptive`（HanziLearn/MathQuiz 已用） | 复用 + 阈值细化 |
| I4 间隔重复调度（SRS） | 错字本/SRS 框架（超范围待授权） | 新调度器 |
| I5 已掌握自动跳过 | — | 新入口逻辑 |
| I6 多模态偏好检测 | A 层（无障碍）+ S 层（语音） | 协同 |
| I7 家长洞察 | E1 家长学习报告（R90） | 扩展字段 |

**结论**：I 层方向与 G+L+S+P+A 高度一致，仅 I1/I4/I5 需新文件/新入口落地（超范围待授权），不回写三核心、不触碰已提交稳态。

---

## 五、升级改造方案精炼（G+L+S+P+A+I 六维商用上线矩阵）

### 5.1 改造目标

- **G（游戏化核心）稳态**：独立玩法/渐进难度/即时反馈/儿童审美/进度成就五项硬指标持续满足。
- **L（商用合规）**：中国《儿童个人信息网络保护规定》+《个保法》+《未保条例》+ 美国 COPPA 2026 文档/配置前置（部署前置，不回写）。
- **S（语音反馈）**：端侧 ASR 零上传 + 宽容阈值 + 温柔降级，默认关可开（超范围待授权）。
- **P（运行性能）**：首屏预算固化（≈235KB gz）+ opencc 按需瘦身 + 长任务拆分 + PWA 离线优先。
- **A（儿童无障碍）**：WCAG2.2AA 底线 + 色不独依 + reduced-motion 总开关 + 44px/200% 缩放 + 视听双通道反馈。
- **I（智能自适应·NEW）**：诊断前置 + 技能级掌握度 + 会话内自适应 + SRS + 自动跳过 + 家长洞察，使游戏化因娃而异。

### 5.2 六维 × 三核心归属表

| 维度 | 汉字 hanzi | 词语 words | 数字 numbers |
|---|---|---|---|
| G | StreakBar×3 / useAdaptive×2 / 字源动画 | WordMatch 11 机制 / 外壳页对齐 | MathLadder/SpeedMath 自适应 / **MathQuiz 最薄弱(2)** |
| L | 合规配置复用 | 合规配置复用 | 合规配置复用 |
| S | 跟读评测（已接 pinyin-pro） | 跟读评测 | 跟读评测（超范围） |
| P | 懒加载已就位 | 懒加载已就位 | opencc/pinyin 大块按需 |
| A | reduced-motion 接入 | reduced-motion 接入 | **44px/对比度治理优先（any 债务最高）** |
| I | useAdaptive 扩展 | 外壳页诊断入口 | **I3 会话内自适应重点（最薄弱页）** |

### 5.3 限现有模块的具体实施内容（不新增越权、复用既有机制）

- **现状分析** → 见第三节技术债务审计（numbers 类型最弱 / words 测试最薄 / 外壳页机制最低）。
- **改造目标** → 六维矩阵稳态 + 三项技术债务收敛（D1/D2/D3）。
- **具体实施** → 复用 `useStreak`/`useAdaptive`/`celebrate`/`StruggleModal`/`BadgeCollection`；I 层仅以新文件/新入口增强（待授权）。
- **验收标准** → 见第六节。

---

## 六、验收标准（技术 7 门禁 + 六维增强）

技术硬门禁（全部须绿，已连续 13 轮满足）：
1. `tsc -b` EXIT=0
2. `vite build` EXIT=0
3. `vitest` ≥1000 全绿（本轮 1057）
4. `lint:budget` ✓ 通过（无规则超基线）
5. 三核心外壳页游戏机制命中 ≥12（R122 已证）
6. 五项硬指标（玩法/难度/反馈/审美/成就）满足
7. 已提交 HEAD 合 main 即触发 deploy-pages（零额外调整）

六维增强验收（属超范围，待授权后落地复核）：
- I1 诊断前置入口、I3 会话内自适应（numbers/MathQuiz 重点）、I4 SRS 调度、I7 家长洞察字段扩展
- D1 numbers any 23→≤10、non-null 29→≤15；D2 words 覆盖比 4.1→≥6；D3 外壳页机制命中≥12

---

## 七、隔离策略与交付

- **仅提交并推送本报告（1 file）**；76 文件 WIP + 三核心零触碰、零回写。
- lint 基线不刷新（RED/通过态均来自 WIP 增量，非已提交代码缺陷）。
- 破坏性操作（如有）前点明风险；本轮无破坏性操作。

**交付物**：`docs/三大模块游戏化升级守门复验与升级改造方案_R136_2026-08-25.md`

---

## 八、未决事项（跨轮持续，R136 补充）

1. 部署阻塞已解除（R122 闭环），合 main 即 deploy-pages。
2. 超范围增强（均待授权）：健康节奏 / 家长仪表盘 / 成就成瘾边界 + 幼小衔接 / E3 书写宽容 / TTS / 商城兑换 / 错字本 / Core Loop 重排 / S 语音层 / P 性能层 / **I 智能自适应层（R136 新增）** / **D1-D3 技术债务收敛（R136 新增）**。
3. 勿对三核心重复回写；稳态已确证，超范围增强以新文件/新入口落地，避免 churn 生产就绪代码。
