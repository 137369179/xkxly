# R160 · 三大模块游戏化升级 — 冒险路线可视化（玩法轮换调度器的儿童向消费层）

> 自动化：`学习乐园加强升级改造方案`（HOURLY，R160 / 2026-08-28 18:00）
> 前置：R159 已落地 `src/game/playVariety.ts`（18 种玩法池 + 六级降级链 + 确定性 seed）
> 本轮：把「不可见的调度引擎」变成「孩子看得懂的冒险路线图」，直接回应需求 #1（独立玩法）与 #5（成长目标感）

---

## 一、本轮实质落地（安全正交 · NEW 文件 · 零触碰用户 WIP）

| 文件 | 角色 | 行数 |
|---|---|---|
| `src/components/gamification/SessionAdventurePath.tsx` | 本节课冒险路线可视化组件（受控、纯展示、零副作用） | ~110 |
| `src/components/gamification/SessionAdventurePath.test.tsx` | jsdom 渲染测试（对齐 GameHud.test.tsx 范式） | 9 项 |

### 设计要点
1. **直接消费 R159 引擎**：`useMemo(() => buildSessionPlan({module, level, recent, seed, length}))`，零重复实现。
2. **把「多样性」呈现给孩子（需求 #1）**：路线图把 18 种玩法中的本节课序列渲染成彩色站点，认/学/读/写/练/测 六类形态一目了然。
3. **渐进式难度可视化（需求 #2/#5）**：难度带用「★数量 + 配色」双通道——低带暖粉(330°)→ 高带青蓝(250°)，孩子看见「越来越厉害」的成长线。
4. **儿童审美（需求 #4）**：圆润果冻气泡 + 入场弹跳动画（`sapPop`，逐站 70ms 错峰），色彩明快。
5. **即时可复现**：`mulberry32 + seed` 确定性，同 seed 渲染结果完全一致（已单测断言），便于课堂重放与回归。
6. **无障碍 / 家长护眼**：`reducedMotion` 入参 + `@media (prefers-reduced-motion: reduce)` 双保险关闭动画；`section/ol/li` 完整 ARIA（`aria-labelledby` / `aria-label` / `aria-current`）；装饰 SVG/符号 `aria-hidden`。
7. **严格隔离接入点（不碰 WIP）**：三核心练习组件 `HanziQuizGame / WordMatch / MathQuiz` 当前为用户未提交 WIP（相对 HEAD 各有 45/59/20 行改动），**本轮零触碰**；接入时只需在练习循环顶部加 `<SessionAdventurePath module level recent seed />`。

### 自测覆盖（9/9）
默认 5 站 / 三模块非空 / 首站 `aria-current=step` / `aria-labelledby` 指向可见标题 / 每站难度 ★(1–3) / `length` 精确控制 / 同 seed 可复现 / `reducedMotion` 移除动画 / **所有站点玩法均落在当前难度带内（绝不越界）**。

---

## 二、五门禁真实结果

| 门禁 | 结果 | 说明 |
|---|---|---|
| `tsc -b` | 我的文件 **0** 错误；全树 11（全为用户 WIP 未跟踪/未提交） | 我的组件 eslint **0 错误 0 警告** |
| `vitest run` | **EXIT=0 · 1331/1331（172 文件）** | 较 R159 的 1295 +36（含用户并发 WIP 测试 + 本轮 9），零回归 |
| `vite build` | 见下方构建任务 | 新组件纯 JSX，esbuild 可编译（WIP 的 tsc 错误不阻断 vite） |
| `lint:budget` | 基线不刷新（我的文件 0 增量） | 延续 R144–R159 不刷新基线策略 |
| git 隔离 | **249 项 WIP 零触碰** | 仅提交本轮 2 代码 + 1 报告 |

> 修复说明：初版测试用 `Partial<SessionAdventurePathProps>` 导致 `createElement` 第二参类型不匹配（TS2769，3 处），改为完整 `SessionAdventurePathProps` 后 tsc 0 错误。

---

## 三、技术债台账（延续 R159，四级）

| ID | 等级 | 描述 | 状态 |
|---|---|---|---|
| TD-R159-01 | **P0** | 用户 WIP 11 个 TS 错误（masteryColorScale/melody 等）阻断 `npm run build` | 用户 WIP，零触碰，仅标注 |
| TD-R159-02 | **P1** | 入口/学习页（WordsPage/NumbersPage）仅挂 `useStreak`，HUD/冒险路线未接入 | 待 WIP 澄清后接入 |
| TD-R160-01 | **P1** | 三核心练习组件（WIP 中）接入 `<SessionAdventurePath />` 展示本节课玩法路线 | 待 WIP 解锁 |
| TD-R160-02 | **P2** | 跨节课 `recent` 历史持久化（localStorage 需走 localStorage-hardening 安全封装） | 组件已支持 `recent` 入参，持久化留待接线层 |

---

## 四、下一步优先级
1. **P0**：待用户 WIP 类型错误清除后，`npm run build` 恢复（自动化不可代修用户 WIP）。
2. **P1**：三核心练习循环顶部接入 `<SessionAdventurePath module level recent seed />`（TD-R160-01）。
3. **P1**：入口/学习页包裹 `GameHud`（R158 已就绪）+ 冒险路线（TD-R159-02）。
4. **P2**：`recent` 跨节课持久化接入安全存储封装。

---

## 五、交付物
- `src/components/gamification/SessionAdventurePath.tsx`（生产就绪组件）
- `src/components/gamification/SessionAdventurePath.test.tsx`（9 项单测全绿）
- 本报告
