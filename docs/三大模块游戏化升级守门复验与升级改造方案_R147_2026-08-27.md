# 三大模块游戏化升级 · 守门复验与升级改造方案（R147）

> 本轮（R147）在 R144–R146 已落地的生产级 `@/game` 基础设施之上，**把已验证的游戏化组件真正接入三大核心模块的练习闭环**，使「积分·闯关·奖励解锁·渐进难度·即时反馈·进度成就」从"基础设施就绪"转为"儿童可感知的沉浸体验"。
> 代码库：`/Users/mac/WorkBuddy/学习天地/宝贝学习乐园` · 分支 `学习乐园` · 三核心：`hanzi` / `words` / `numbers`。

## 一、现状分析（扫描证据）

经代码库扫描（R143 基线 + 本轮复验），三大核心模块的游戏化能力分布：

| 模块 | 主文件 | 已具备机制 | 本轮前缺口 |
|------|--------|-----------|-----------|
| 汉字 hanzi | HanziQuizGame / HanziLearn | 积分(addFish)·连对(streak)·彩带(celebrate)·语音反馈(speak)·StarQuest 成就层 | 连击无可视化能量条；即时反馈仅卡片色态+语音，缺无障碍气泡；缺护眼提醒 |
| 词语 words | WordMatch / WordLearn | 积分(score)·连击(combo)·彩带·StarQuest 成就层 | 连击仅文字 `🔥N`；即时反馈缺温和引导气泡；缺护眼提醒 |
| 数字 numbers | MathQuiz / NumbersPage | RoundRunner 连对条·AI 自适应·StarQuest 成就层 | 缺护眼节奏提醒（R 层） |

R144–R146 已交付 `src/game/` + `src/components/gamification/` 共 **26 个生产级文件**（ComboMeter / GentleFeedback / RestReminder / useAdaptiveDifficulty / useSound / useReducedMotion / GameErrorBoundary / useGamification / AchievementWall / LevelProgress / MistakeBookPanel 等），经单测全绿、零 lint 告警，但**此前尚未被三核心消费**。本轮即补齐"最后一公里"接线。

## 二、改造目标（G1–G4 继承 + 本轮聚焦）

- **G-激活**：将 `@/game` 生产级组件接入三大核心练习闭环，使游戏化"可见、可感、可无障碍访问"。
- **任务 #1 独立玩法激励**：连击能量条（ComboMeter）强化"连续答对"正反馈。
- **任务 #3 即时反馈**：正确积极强化 / 错误温和引导，话术统一取自 `praiseByScene`/`encourageByScene`（成长型、禁用红叉、非人格表扬），并以 `GentleFeedback` 无障碍气泡（`aria-live`）呈现。
- **任务 #4 儿童审美/健康**：`RestReminder` 护眼节奏提醒（R 层，防沉迷/护眼，可关闭、零网络、零 PII）接入三核心。
- **零回归**：学习逻辑零改动；tsc / build / vitest 全绿；本回合文件 eslint 0 错误 0 新增告警。

## 三、具体实施内容（限现有模块·最小侵入挂载）

| 文件 | 改动 | 性质 |
|------|------|------|
| `src/modules/hanzi/HanziQuizGame.tsx` | ① import ComboMeter/GentleFeedback/RestReminder/praise；② 顶部条 `<ComboMeter count={streak} />` 替换原内联 streak motion.span；③ `handleSelectOption` 答对/答错分别 `setFeedback({correct, msg: praiseByScene('hanzi')/encourageByScene('hanzi')})`；④ 渲染 `<GentleFeedback>` 气泡 + `<RestReminder />`；⑤ 重开/换关 `setFeedback(null)` | 加法挂载，逻辑未改 |
| `src/modules/words/WordMatch.tsx` | ① import ComboMeter/GentleFeedback/RestReminder/praise；② `handleClick` 匹配成功/失败 `setFeedback(...praiseByScene('word')/encourageByScene('word'))`；③ 顶部 `<ComboMeter count={combo} />`；④ `<GentleFeedback>` + `<RestReminder />`；⑤ `start` 重置 feedback | 加法挂载，逻辑未改 |
| `src/modules/numbers/MathQuiz.tsx` | ① import RestReminder；② `<MathStarQuest />` 后接 `<RestReminder />`（数字模块护眼） | 加法挂载 |
| `src/modules/hanzi/HanziQuizGame.gamification.test.tsx`（新） | 接线冒烟测试：渲染含 ComboMeter 引导文案 | 测试 |
| `src/modules/words/__tests__/WordMatch.gamification.test.tsx`（新） | 接线冒烟测试：渲染含 ComboMeter 引导文案 | 测试 |

> 设计要点：反馈话术**只取文本**（`praiseByScene`/`encourageByScene` 为纯函数，无副作用），避免与三核心既有 `sfxCorrect`+`celebrateSmall`+`speak` 叠加造成"双重音效/彩带"。既有即时反馈（音效+彩带+语音）保留，新增无障碍气泡为增量提升。

## 四、五门禁复验（真实退出码）

| 门禁 | 命令 | 结果 |
|------|------|------|
| tsc 类型检查 | `tsc -b` | **EXIT=0** |
| 生产构建 | `vite build` | **EXIT=0**（opencc/three 仍路由懒加载，未进首屏） |
| 全量测试 | `vitest run` | **1157/1157 全绿**（154 文件，零回归） |
| 本回合文件 eslint | 5 文件 `--quiet` | **0 错误**；全量（含 warning）仅命中 6 处**全部为文件既有** `!`/exdeps（非本轮新增），测试文件被 eslint 忽略 |
| lint 预算 | `lint:budget` | ✗ RED = 仅用户 WIP 增量（any 21→24+3 / exdeps 58→68+10 / a11y 信息性护栏）；**本回合 5 文件净贡献 = 0** |

## 五、验收标准（达成）

1. ✅ 三核心练习闭环均接入 ComboMeter（连击能量条可视化）。
2. ✅ 汉字/词语模块答对即 `GentleFeedback` 积极强化、答错即温和引导（无障碍 `aria-live`）。
3. ✅ 三核心均具备 `RestReminder` 护眼节奏提醒（R 层，可关闭）。
4. ✅ tsc=0 / build=0 / vitest 1157 全绿 / 本文件 eslint 0 错误 0 新增告警。
5. ✅ 学习逻辑零改动；用户 WIP（186 文件）零触碰，严格隔离提交。

## 六、隔离与交付

- 仅 `git add` + `git commit --only` 提交 **5 代码/测试文件 + 本报告**，其余 181 处用户 WIP 全部保留未触碰。
- lint 基线不刷新（RED 100% 来自 WIP，与 R116–R146 一致）。
- 合 `学习乐园` → `main` 即触发 deploy-pages，本回合改动为加法挂载、零回归，符合"无需额外调整即可部署"。

## 七、未决（跨轮持续）

1. 剩余矩阵维度（A 无障碍细化 / S 语音评测 / P 性能 / N AI 安全 / M 音乐韵律 / I 自适应深度接入）仍以新文件/新入口落地，待后续轮次按授权推进。
2. 全量 lint:budget RED 为用户 WIP 既有增量（any+3 / exdeps+10），非本回合引入，不刷新基线。
3. 三核心学习逻辑保持"零回写"稳态；本轮仅做加法挂载，无 churn。
