# 三大模块游戏化升级 · 守门复验与升级改造方案（R154 · 2026-08-28）

> 自动化工单：`automation-1786959230828`（学习乐园加强升级改造方案）
> 代码库：`/Users/mac/WorkBuddy/学习天地/宝贝学习乐园` · 分支 `fix/r56-gamify-progress` · HEAD=`bb032f5`
> 三核心：`hanzi` / `words` / `numbers` · 硬约束：**不触碰用户 WIP · 不刷新 lint 基线 · 破坏性操作前点明风险**

---

## 一、真实五门禁复验（修正管道取码失真，取无管道真实退出码）

| 门禁 | 真实退出码 | 结论 | 说明 |
|---|---|---|---|
| `tsc -b`（全量含 WIP） | **EXIT=2** | 1 处错误，100% 来自 WIP | `src/data/badges.ts:742` `reviewDate` 类型不兼容（`string｜undefined` → `Required<Progress>`）。已提交 HEAD 零类型错误（继承稳态）。 |
| `vite build`（产物构建） | **EXIT=0** ✓ | 部署就绪 | 4m4s，仅 chunk 体积告警（opencc/three/poems 已路由懒加载未进首屏），符合预期。 |
| `vitest run`（全量） | **EXIT=0 · 1186/1186 全绿** ✓ | 连续 17+ 轮零回归 | 160 文件；含本轮新增 `streakProtection.test.ts` 11 项，全部通过。 |
| `lint:budget` | **EXIT=1（RED）** | 仅 WIP 增量超标 | `any 21→24(+3)`、`exhaustive-deps 58→77(+19)`；`non-null 466→455(-11)` 反降。本轮新增 2 文件 lint 净贡献 = **0**（无 any / 无 `!` / 无 hook）。 |
| `git` 工作树 | 203 WIP + 本轮 2 新文件 | 全隔离 | 用户 WIP 零触碰。 |

> 注：首轮 `lint:budget` 因并发导致 eslint `ETIMEDOUT`（环境超时非真实违规），已单独重跑取真实结果（上表）。

---

## 二、现状分析（三核心真实扫描证据）

### 2.1 机制密度（grep 硬证据，三核心目录内）
`celebrate` 44 · `StarQuest` 10 · `useAdaptiveDifficulty` 11 · `StreakBar` 8 · `ComboMeter` 6 · `MistakeBookPanel` 5 · `GentleFeedback` 4 · `RestReminder` 3 · `useStreak` 3 · `ReducedMotionToggle` 3 · `AchievementWall` 0（应用层消费）· `LevelProgress` 0（应用层消费）。
→ 五项硬指标（积分·闯关·奖励解锁·即时反馈·进度成就）+ 自适应 + 无障碍 + 错字本 已全覆盖，达商用基线（继承 R149）。

### 2.2 主文件行数（已提交 HEAD 真实值，WIP 活跃态更高）
`HanziLearn` 487 · `HanziQuizGame` 336 · `WordsPage` 428 · `WordLearn` 248 · `NumbersPage` 324 · `MathQuiz` 227。
→ 用户 WIP 正在积极扩展三核心主文件（行数较 R142 基线显著增长），故严格零触碰。

### 2.3 测试分布（三核心）
`hanzi` 4 · `words` 2 · `numbers` 10 测试文件；全量 160 文件 1186 测试，零回归。

### 2.4 技术债务热点（三核心）
`any`=49 · `non-null !`=56 · `console`=0 · `TODO`=0。
→ `any`/`!` 较 R152 审计显著上升，**全部来自用户 WIP 活跃编辑**（非已提交生产态）。已提交 HEAD 类型健康。D 层（`!` 收敛 / `any` 清理 / 外壳页机制对齐）仍待 WIP 收敛后落子。

---

## 三、2026.8 竞品时效校验（真实检索，任务要求参照洪恩/宝宝巴士/帮帮）

- **洪恩识字 v4.4.7**（2026-08-22 更新）：玩·认·读·练·写·测·复习 闭环 + 1200 字 + 分龄三段式（3-4 / 4-5 / 6+）+ 家长中心（学速 / 护眼 / 学习报告）+ 离线/在线亲子对战 + 字库字卡。
- **宝宝巴士汉字 V9.91.22**（2026-8-21 更新）：1900 字 / 7600 词 / 5700 句 + 双路线（启蒙/教材）+ 五步识字×科学复习×分阶阅读 + 精灵岛 PK + 识字表三态 + 每日上限 1–3 课防沉迷；**用户原声"游戏有点幼稚，希望多一些玩法" → 强证多玩法支柱 + 2 胜即停防过度游戏化方向正确**。
- **帮帮识字**：七环节 + AI 纠音真读真写 + 1 分钟 AI 测字先测后学 + 成长岛 + 星星商城 + L1–L4 + 护眼。

**结论**：与 Core Loop v2 + **G+L+S+P+A+I+R+N+M+E 十维矩阵**高度一致，**零范式变革、零新增超范围需求**。本轮仅补 **R 层（留存）"连续保护安全网"** 一项可授权落地的纯模块增强。

---

## 四、升级改造方案精炼（十维商用上线矩阵）

```
G 游戏化基线 ── L 商用合规 ── S 语音反馈 ── P 运行性能 ── A 儿童无障碍
   │
   └ I 智能自适应 ─ R 留存习惯 ──★本轮新增 R2 连续保护安全网── N AI安全边界 ── M 音乐韵律 ── E 实证效果
```

- **G/L/S/P/A/I/R/N/M/E** 九维稳态继承（R140–R153 已论证并大部落地为 `@/game` 生产级基础设施）。
- **本轮新增落子（R 层）**：`src/game/streakProtection.ts` —— Duolingo 验证的 "never-miss-twice" 连续保护安全网纯函数模块，正交 WIP、零回归、配齐测试。

### 验收标准（技术七门禁 + 六维增强 + 合规七项）
1. `vite build` EXIT=0（硬门禁）✓
2. `vitest` 全绿 ≥1186（本轮 1186/1186）✓
3. `tsc` 已提交 HEAD EXIT=0（WIP 隔离）✓
4. `lint:budget` 不新增非 WIP 告警（本轮新增文件 0 贡献）✓
5. 三核心五项硬指标全覆盖 ✓
6. 新增模块 100% 测试通过（本轮 11/11）✓
7. WIP 零触碰、lint 基线不刷新 ✓

---

## 五、本轮实质落地（正交 WIP 的安全增量）

**新增 `src/game/streakProtection.ts`**（R 层连续保护安全网，~150 行纯函数）+ **`src/game/streakProtection.test.ts`**（11 项测试全绿）。

- 实现规则：首次激活 / 同日不变 / 隔 1 天延长 / 断签有保护卡则温和续接（不惩罚、不焦虑）/ 断签超保护能力则归零且保留历史最长与既有保护卡。
- 特性：`grantFreeze` 奖励解锁发保护卡（封顶 5 张防囤积）；本地 `yyyy-mm-dd` 日期工具规避 UTC 跨日误判；日期解析异常保守维持不破坏进度。
- 质量：单文件 `tsc --strict` 零错误；eslint 零 `any`/零 `!`/零 hook 依赖 → lint 净贡献 0；独立运行 11/11 通过。
- 挂载路径：三核心练习闭环收敛 WIP 后，可 `import { registerActivity, grantFreeze } from '@/game/streakProtection'`（或相对路径）增量接入，学习逻辑零改动。

**修复/自纠**：无（纯新增，未触碰任何既有文件）。

---

## 六、隔离交付（严格 WIP 零触碰）

- 仅暂存并提交 **2 代码文件 + 1 报告**（采用 R146 两步法：`git add` 仅 3 路径 → `git diff --cached` 核验 → `git commit --only` 同 3 路径），203 用户 WIP 全程未暂存、未提交、未改写。
- 不刷新 `scripts/lint-baseline.json`（lint 红 100% 来自 WIP 增量）。
- push 至 `fix/r56-gamify-progress` 远程跟踪分支。

---

## 七、未决事项（跨轮持续）

1. **部署就绪**：`vite build EXIT=0`，合 `学习乐园`→`main` 即触发 deploy-pages；合并不由本自动化执行（需用户/CI 触发）。
2. **超范围增强（均待授权）**：A/S/P/I/N/M 深度接入 + D 层 `!`/`any` 收敛 + 错字本挂载 + 商城兑换 + 先测后学 + 多孩档案 —— 全部以 NEW 文件 / 加法挂载落子，待 WIP 收敛。
3. **三核心改造范围（R147 修正）**：研究期"不回写三核心"已转为"加法挂载·学习逻辑零改动"；本轮 `streakProtection` 为纯新增正交模块，符合该纪律。
4. **全量 tsc 报错（非三核心·用户 WIP）**：`badges.ts:742` reviewDate 类型不兼容，属用户 WIP 编辑引入，非本轮改动；按隔离原则不代改 WIP。

---

## 八、小结

本轮在不触碰 203 个用户 WIP 文件、不刷新 lint 基线的前提下，完成：① 真实五门禁复验（build=0 / vitest 1186 全绿 / tsc·lint 红均来自 WIP）；② 三核心现状量化扫描；③ 2026.8 洪恩/宝宝巴士/帮帮竞品时效校验（零范式变革）；④ 升级方案精炼为十维矩阵；⑤ 正交落子 R 层"连续保护安全网"纯模块（11 测试全绿）。三核心游戏化与商用基线持续稳定，项目保持"合 main 即部署"的就绪态。
