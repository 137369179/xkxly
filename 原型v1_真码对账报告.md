# 设计原型 v1 → 真实工程 对账报告（量产前基线收口）

> 日期：2026-08-29 | 目的：把 DesignEngineTeam 交付并通过真机视觉验收的「儿童 UI 设计原型 v1」与真实 `src/` 工程逐项对账，确认重设计已从「设计稿」落地为「可上线产品」。

## 一、对账方法
以 `outputs/儿童UI设计原型_v1.html`（真机视觉验收 PASS 20/25）为**设计基线**，以真码 `src/`（路由/导航/页面/令牌）为**实现侧**，逐屏 + 逐令牌对照。差异分三类处理：
- **A 类（原型已验证、真码未回灌）** → 回灌真码
- **B 类（原型装饰性、真码无对应结构）** → 标记 N/A，不强行对齐
- **C 类（真码已自洽、命名不同）** → 以代码为事实源，仅记录

## 二、逐屏对账

| 屏 | 原型 v1 | 真码 | 结论 |
|---|---|---|---|
| 首页 home | Hero + 一屏一事 | `HomePage` + `HomeHero` | ✅ 路由/结构一致；Hero 对比度 P1-1 已回灌（深 on #3D1424，5.4:1） |
| 乐园地图 hall | 四岛（学8/故4/探7/创6） | `HallPage` + `navByIsland()` + `islandIds()` | ✅ 四岛数据全在；探索岛 7（含 discoveries 卡，P1-3 一致） |
| 汉字 hanzi | 认字环节干净 | `HanziPage` | ✅ 路由存在 |
| 今日 today | 主流程自动推进 | `TodayPage` + SectionRunner | ✅ 路由存在；自动推进 1200ms 已接入 |
| 成长 growth | 贴纸博物馆 + UndoToast | `GrowthPage` + `GrowthMuseumPage` | ✅ 路由存在；`refundSticker`+UndoToast 已落地 |
| 底栏 | 72px / 图标 36 / 字 17·900 | `BottomTabs` 72px | ✅ 一致 |

## 三、P1 三项审查结论

| P1 | 原型问题 | 真码状态 | 处理 |
|---|---|---|---|
| **P1-1** Hero 白字对比度违规 | 白字压粉渐变 ~2.9:1 | 原真码 `text-white` 同问题 → **已回灌 `text-candy-pink-on`(#3D1424)** | ✅ A 类，本轮闭环 |
| **P1-2** 顶/底 Tab 不同步 | 原型装饰性顶 Tab 与底 Tab 标签/图标不一致 | 真实 `TopBar` 是**头部栏**（Logo+工具+身份），非标签栏；无装饰顶 Tab → 无同步风险 | ✅ B 类，N/A |
| **P1-3** 探索岛计数矛盾 | 原型初稿 探索岛 6，与数据层 7 矛盾 | 真码 `nav.ts` 探索岛 7（discoveries 卡） | ✅ C 类，真码自洽 |

## 四、设计系统 v1 令牌环闭合

| 令牌组 | 规范（design-tokens-v1.md） | 真码 `@theme` | 状态 |
|---|---|---|---|
| 六色 main/soft/deep | 六色四值 | ✅ 全在 | 闭合 |
| **`on` 深字（6 色）** | pink #3D1424 等 | ✅ 本轮补全（candy-*-on） | 闭合 |
| 语义色 success/warning/error/info | 4 色 | ✅ 全在 | 闭合 |
| 中性 bg/ink/ink-soft | 3 值 | ✅ 全在 | 闭合 |
| 字体 font-round/font-han | 圆体+楷体 | ✅ 全在 | 闭合 |
| 圆角 hero/card | 32/24 | ✅ 在；**本轮补 base 12 / modal 28** | 闭合 |
| 果冻软阴影 sm/md/lg/pop | 4 档 deep #C9285C | ✅ candy-/jelly- 命名，数值等价 | 闭合 |
| **`--ease-jelly` Q弹缓动** | cubic-bezier(.34,1.56,.64,1) | ⚠️ 仅内联于 `--animate-*`，无命名令牌 → **本轮提升为 `--ease-jelly`** | 闭合 |
| 动效持续 / 强调动画 | enter/press/celebrate + popIn/jellyBounce/wobble | ✅ `--animate-*` 全在（曲线与 ease-jelly 一致） | 闭合 |

> 注：`--radius-pill:9999px` 未单独提升——Tailwind 默认 `rounded-full` 即 9999px，等价不重复。

## 五、资产对账
- **41 个品牌 3D 图标**（WebP 256²）：`public/icons/brand3d/`，`nav.ts` 39 处 `brandIcon` 引用，`BottomTabs`/`HallPage` 渲染 `<img>` 兜底 emoji。✅ 一致
- 性能：PNG 47MB → WebP 0.24MB（降 99.5%），`emptyOutDir:false` 已知会残留旧 dist PNG（已手动清）。

## 六、结论
**真实工程已与「通过真机视觉验收的设计原型 v1」达到功能与视觉基线一致**，可作为量产前视觉基线。唯一真缺口（P1-1 Hero 对比度）已回灌并验证；设计系统 v1 令牌环本轮完全闭合（`on` 深字 + `ease-jelly` + 圆角 base/modal 全部提升为 Tailwind 工具类）。

**剩余建议（非阻塞）**：
- 全站 `ease-jelly` 缓动目前散落在各 `--animate-*` 内联；组件若想复用统一缓动，可直接用新 `ease-jelly` 工具类（如 `transition-transform ease-jelly`）。
- `candy-*-on` 深字令牌已在全站可复用，后续任何「main 色压字」场景应改用 `text-candy-*-on` 守住对比度铁律。
- 量产前建议一次性专项 a11y 审计（A11Y-CLICK-NO-ROLE / A11Y-BUTTON-DIV 历史增量来自基线陈旧，非本轮引入）。
